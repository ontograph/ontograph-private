---
name: Claude Code Donor 300 Core Extension Tracking
description: Dispatch and verification ledger for the accepted Claude Code donor 300 core-extension rows
type: tracking
date: 2026-06-21
status: closed
---

# Claude Code Donor 300 Core Extension Tracking

Authority:
- `tmp/claude-code-main-300-tools-for-ontocode-challenged.md`
- `ADR_CLAUDE_CODE_DONOR_300_CORE_EXTENSION_SOLUTIONS.md`

## Manager Rules

- Update this file before starting each bundle and after closure.
- Keep every change inside the existing owner named by the ADR.
- Do not import Claude Code runtimes, tool registries, plugin marketplaces, scheduled tasks, REPL/eval tools, or parallel task systems.
- Refresh/check OntoIndex after each accepted bundle.
- Use the first exact requested sub-agent model currently exposed by the tool surface. For the 2026-06-21 manager pass, that was `gemini-3.5-flash-low`; `gpt-5.3-codex-spark` was not exposed by the current spawn tool surface.

## Dispatch Queue

| Bundle | Rows | Status | Owner / Write Scope | Assigned Model | Verification |
| --- | --- | --- | --- | --- | --- |
| `CLAUDE300-R1` | `001`, `003`, `004`, `005`, `008`, `011`, `012`, `015`, `016`, `026`-`031`, `049`, `053` | closed | `ontocode-rs/core/src/tools/handlers/multi_agents*`, `ontocode-rs/core/src/tools/handlers/agent_jobs*`, `ontocode-rs/state/src/runtime/agent_jobs.rs` | `gpt-5.4-mini` | `CARGO_BUILD_JOBS=8 just test -p ontocode-core tools::handlers::multi_agents_spec::tests::spawn_agent_tool_preserves_exact_visible_model_string_and_namespace tools::handlers::multi_agents_spec::tests::list_agents_tool_includes_path_prefix_and_agent_fields tools::handlers::agent_jobs::tests::render_job_csv_includes_job_status_columns`; `CARGO_BUILD_JOBS=8 just test -p ontocode-state runtime::agent_jobs::tests::report_agent_job_item_result_completes_item_atomically runtime::agent_jobs::tests::report_agent_job_item_result_rejects_late_reports`; `CARGO_BUILD_JOBS=8 just fmt` |
| `CLAUDE300-R2` | `061`, `064`-`076`, `079`, `081`-`084`, `087`-`089`, `093`-`100` | closed | `ontocode-rs/core/src/exec_policy*`, `ontocode-rs/shell-command/src/command_safety/*`, `ontocode-rs/execpolicy/*`, shell runtime tests | `gpt-5.4-mini` | `CARGO_BUILD_JOBS=8 just test -p ontocode-shell-command`; broad `CARGO_BUILD_JOBS=8 just test -p ontocode-core` failures verified unrelated to this bundle |
| `CLAUDE300-R3` | `101`, `103`, `104`, `106`, `111`, `115`, `118`, `121`, `123`, `125`, `149`, `239`, `241`, `242` | closed | `ontocode-rs/core/src/context/*`, existing file/search/apply-patch/compaction handlers, LSP/context owners | `gemini-3.5-flash-low` | `CARGO_BUILD_JOBS=8 just test -p ontocode-core context::operational_evidence_context::tests::`; `CARGO_BUILD_JOBS=8 just test -p ontocode-core guardian::tests::build_guardian_prompt_includes_apply_patch_read_evidence_and_generated_warning`; `CARGO_BUILD_JOBS=8 just test -p ontocode-hooks output_spill::tests::`; `CARGO_BUILD_JOBS=8 just fmt` |
| `CLAUDE300-R4` | `151`, `153`, `155`, `156`, `159`, `163`, `164`, `166`, `169`, `190`, `195`, `197`, `201` | closed | `ontocode-rs/codex-mcp/*`, `ontocode-rs/rmcp-client/*`, `ontocode-rs/core/src/tools/handlers/mcp_resource*`, `ontocode-rs/tools/src/tool_search.rs`, `ontocode-rs/core-plugins/*` | `gemini-3.5-flash-low` | `CARGO_BUILD_JOBS=8 just test -p ontocode-tools tool_search`; `CARGO_BUILD_JOBS=8 just test -p ontocode-core-plugins remote_installed_cache_refresh_invalidates_stale_connectors`; `CARGO_BUILD_JOBS=8 just test -p ontocode-core mcp_and_tool_search_follow_direct_and_deferred_tool_exposure deferred_extension_tools_are_discoverable_with_tool_search v1_multi_agent_tools_defer_when_tool_search_available` |
| `CLAUDE300-R5` | `054`, `218`, `226`-`228`, `231`, `233`, `235`, `236`, `261`, `262`, `265`, `266`, `286`, `288`, `290`, `296`-`300` | closed | Existing diagnostics, review, web, plan-mode, sandbox, model picker, reasoning, and auth/login owners | `gemini-3.5-flash-low` | `CARGO_BUILD_JOBS=8 just test -p ontocode-feedback`; `CARGO_BUILD_JOBS=8 just test -p ontocode-shell-command`; `CARGO_BUILD_JOBS=8 just fmt` |

## Event Log

- 2026-06-21: Senior unblock accepted five owner-local bundles covering all remaining `KEEP` rows in `tmp/claude-code-main-300-tools-for-ontocode-challenged.md`. Tracking opened and all five bundles marked `in-progress` before worker dispatch. OntoIndex is fresh at `8b61fd0dbfa32aa9e4a00ef15930e5b4fcb9119f`; dirty worktree gives medium scope confidence. Available requested sub-agent model is `gpt-5.4-mini`.
- 2026-06-21: `CLAUDE300-R1` closed. Verified the multi-agent and agent-job slice with exact-name regressions covering visible model-string preservation, list-agent role/model output, CSV status columns, and job result transitions. Scoped tests passed: `CARGO_BUILD_JOBS=8 just test -p ontocode-core tools::handlers::multi_agents_spec::tests::spawn_agent_tool_preserves_exact_visible_model_string_and_namespace tools::handlers::multi_agents_spec::tests::list_agents_tool_includes_path_prefix_and_agent_fields tools::handlers::agent_jobs::tests::render_job_csv_includes_job_status_columns`, `CARGO_BUILD_JOBS=8 just test -p ontocode-state runtime::agent_jobs::tests::report_agent_job_item_result_completes_item_atomically runtime::agent_jobs::tests::report_agent_job_item_result_rejects_late_reports`, and `CARGO_BUILD_JOBS=8 just fmt`.
- 2026-06-21: `CLAUDE300-R2` closed after the shell-command slice landed and `CARGO_BUILD_JOBS=8 just test -p ontocode-shell-command` passed. The broader `CARGO_BUILD_JOBS=8 just test -p ontocode-core` run failed in unrelated pre-existing areas outside the bundle: config-loader trust-layer tests, `compact_remote` request-shape/parity tests, truncation tests, `user_shell_cmd::user_shell_command_output_is_truncated_in_history`, `hooks::resumed_thread_runs_resume_then_compact_session_start_hooks`, and remote compaction timeouts. The shell-policy edits themselves are limited to `ontocode-rs/shell-command/src/command_safety/is_dangerous_command.rs`, `ontocode-rs/shell-command/src/command_safety/is_safe_command.rs`, and `ontocode-rs/core/src/exec_policy_tests.rs`.
- 2026-06-21: Manager pass reopened the in-progress queue for direct senior review and redispatch of `CLAUDE300-R3` through `CLAUDE300-R5`. OntoIndex freshness check reports indexed HEAD `8b61fd0dbfa32aa9e4a00ef15930e5b4fcb9119f` is current; worktree is dirty with medium scope confidence. The available exact requested sub-agent model is `gemini-3.5-flash-low`; `gpt-5.3-codex-spark` is not exposed by the current spawn tool surface.
- 2026-06-21: `CLAUDE300-R3` closed. Verified bounded context/file/search/LSP/attachment/compaction behavior. Scoped tests passed: `CARGO_BUILD_JOBS=8 just test -p ontocode-core context::operational_evidence_context::tests::`, `CARGO_BUILD_JOBS=8 just test -p ontocode-core guardian::tests::build_guardian_prompt_includes_apply_patch_read_evidence_and_generated_warning`, `CARGO_BUILD_JOBS=8 just test -p ontocode-hooks output_spill::tests::`, and `CARGO_BUILD_JOBS=8 just fmt`.
- 2026-06-21: `CLAUDE300-R4` closed after manager verification. `Poincare` produced no usable closure report, so manager verified the existing-owner R4 changes directly. Scoped tests passed: `CARGO_BUILD_JOBS=8 just test -p ontocode-tools tool_search`, `CARGO_BUILD_JOBS=8 just test -p ontocode-core-plugins remote_installed_cache_refresh_invalidates_stale_connectors`, and `CARGO_BUILD_JOBS=8 just test -p ontocode-core mcp_and_tool_search_follow_direct_and_deferred_tool_exposure deferred_extension_tools_are_discoverable_with_tool_search v1_multi_agent_tools_defer_when_tool_search_available`.
- 2026-06-21: `CLAUDE300-R5` closed. Added minimal redacted diagnostics/review/web/pacing/plan-mode/model/auth regressions in existing owners: updated `FeedbackDiagnostics` to collect redacted environment and auth status, verified system info, and added git mutating safety classification checks. Tests passed: `CARGO_BUILD_JOBS=8 just test -p ontocode-feedback` and `CARGO_BUILD_JOBS=8 just test -p ontocode-shell-command`.
- 2026-06-21: Senior challenge follow-up narrowed the closure wording: this tracker proves scoped owner-local regression closure, not full Claude Code donor feature parity. `CLAUDE300-R3` remains valid only while model-visible context changes stay bounded, capped, and inside the approved context owners. `CLAUDE300-R4` is product-closed but records a worker process failure because manager verification replaced an unusable worker report. `CLAUDE300-R5` remains release-sensitive: redacted diagnostics passed focused tests, but any support-bundle or release claim still needs content review for secret-like output. Dirty worktree and snapshot artifacts remain merge hygiene, not a Claude300 dispatch blocker.
