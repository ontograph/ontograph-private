# Ontocode Full Legacy Migration Tracking

Source plan: `ONTOCODE_FULL_LEGACY_MIGRATION_PROJECT_PLAN.md`

Date opened: 2026-06-14
Status: active-stage-5

## Rules

- Update this tracking file before starting each task.
- Use OntoIndex before code-symbol edits.
- Refresh OntoIndex after each completed accepted task.
- Do not start implementation stages until F0 surface matrix is complete and reviewed.
- Sub-agents must use only `gpt-5.3-codex-spark` or `gpt-5.4-mini`; if the runtime cannot enforce model choice, the agent must report that limitation.
- Workers must use disjoint write scopes.
- No broad find-and-replace.

## Task Ledger

| ID | Stage | Scope | Status | Owner | Write Scope | OntoIndex Evidence | Verification | Rollback |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F0 | Approval and inventory | manager gate and matrix integration | done | manager | `ONTOCODE_FULL_LEGACY_MIGRATION_TRACKING.md`; `ONTOCODE_FULL_LEGACY_MIGRATION_STAGE0_REVIEW.md` | Stage 0 matrices accepted; index refreshed after each accepted task; exact-symbol impact pending before code edits | doc diff check passed | no code changes |
| F0-A | Stage 0 inventory | workspace layout and build graph matrix | done | Darwin | `ONTOCODE_FULL_LEGACY_MIGRATION_MATRIX_LAYOUT.md` | OntoIndex search used by worker; exact impact still required before code edits | `git diff --check` passed | delete matrix file |
| F0-B | Stage 0 inventory | CLI command and helper/runtime matrix | done | Hubble | `ONTOCODE_FULL_LEGACY_MIGRATION_MATRIX_CLI_HELPERS.md` | worker recorded OntoIndex lock; exact impact still required before code edits | `git diff --check` passed | delete matrix file |
| F0-C | Stage 0 inventory | package, SDK, state/env, protocol, telemetry matrix | done | Russell | `ONTOCODE_FULL_LEGACY_MIGRATION_MATRIX_PACKAGES_STATE_PROTOCOL.md` | worker recorded OntoIndex lock; exact impact still required before code edits | `git diff --check` passed | delete matrix file |
| F0-D | Stage 0 integration | manager go/no-go review and dispatch decision | done | manager | `ONTOCODE_FULL_LEGACY_MIGRATION_STAGE0_REVIEW.md` | refreshed index after F0-A/F0-B/F0-C; exact impact pending before code edits | doc diff check passed | no code changes |
| F0-E | Senior unblock | dirty worktree classification and next safe dispatch decision | done | manager | `ONTOCODE_FULL_LEGACY_MIGRATION_TRACKING.md`; `ONTOCODE_FULL_LEGACY_MIGRATION_STAGE0_REVIEW.md` | OntoIndex refreshed after accepted unblock tasks | doc diff check pending | no code changes |
| F0-E-A | Senior unblock | dirty worktree isolation options | done | Aristotle | `ONTOCODE_FULL_LEGACY_MIGRATION_DIRTY_WORKTREE_UNBLOCK.md` | worker recorded dirty-worktree status; manager refreshed OntoIndex separately after accepted tasks | `git diff --check` passed | delete unblock file |
| F0-E-B | Senior unblock | F1 path/layout preflight from accepted matrices | done | Peirce | `ONTOCODE_FULL_LEGACY_MIGRATION_F1_PREFLIGHT.md` | worker used Stage 0 matrices and recorded exact pre-edit OntoIndex checks | `git diff --check` passed | delete preflight file |
| F0-E-C | Senior unblock | release/versioning owner gate decisions for F4-F6 | done | Archimedes | `ONTOCODE_FULL_LEGACY_MIGRATION_RELEASE_VERSIONING_GATES.md` | worker recorded OntoIndex context targets and required disambiguation before edits | `git diff --check` passed | delete gates file |
| F1 | Workspace layout rename | `ontocode-rs/` -> `ontocode-rs/` | done | Bohr + manager | clean sibling worktree `/opt/demodb/_workfolder/ontocode-f1-layout` | clean sibling worktree verified; OntoIndex CLI refresh run with explicit worktree `cd`; pre-edit batch impact union risk MEDIUM; no unresolved high/critical risk | `git diff --check`, `cargo metadata --manifest-path ontocode-rs/Cargo.toml --no-deps`, `just fmt`, `just test -p codex-cli`, and `cargo build --manifest-path ontocode-rs/Cargo.toml -p codex-cli --bin ontocode` passed | remove sibling worktree/branch |
| F1-A | Workspace layout prerequisite | missing helper entrypoint files required by existing bin targets | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/exec-server/src/main.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/windows-sandbox-rs/src/main.rs` | OntoIndex could not resolve new helper wrapper symbols; bounded manager fix only adds files already referenced by manifests | `just fmt` passed after adding wrappers | delete the two added files |
| F2 | CLI cutover | public command behavior | done | Anscombe + manager | CLI/tests/Bazel command surfaces in `/opt/demodb/_workfolder/ontocode-f1-layout` | F1 accepted; pre-edit impact found CRITICAL/HIGH risk; proceeded under approved ADR with narrow CLI-only scope | `just fmt`, `just test -p codex-cli --test ontocode_alias`, `cargo build --manifest-path ontocode-rs/Cargo.toml -p codex-cli --bin ontocode`, expected `--bin codex` failure, `bazel query //ontocode-rs/cli:ontocode`, and `git diff --check` passed | restore `codex` shim |
| F3 | Helper cutover | runtime helper binaries and paths | done | Singer | arg0/sandbox/exec/app-server/TUI consumers in `/opt/demodb/_workfolder/ontocode-f1-layout` | pre-edit impact found CRITICAL/HIGH risk; implementation kept field-name quarantine and changed helper executable outputs/values only | `just fmt`, `just test -p codex-arg0`, `codex-shell-escalation`, `codex-linux-sandbox`, `ontocode-exec-server`, and `git diff --check` passed; Windows runtime not executed on Linux | restore helper aliases |
| F4 | Package migration | npm/Python/SDK/native packages | done | manager | package/release tooling in `/opt/demodb/_workfolder/ontocode-f1-layout` | pre-edit impact union MEDIUM; preserved package identities/import paths while cutting staged/runtime binary surfaces to `ontocode` | `just fmt`, `python3 -m unittest scripts.codex_package.test_cargo`, `git diff --check` passed; `sdk/python` artifact workflow requires Python 3.11 `tomllib` and could not be fully verified on this host | restore old package |
| F4-A | Package migration | `core` crate package identity rename to `ontocode-core` with compatibility alias | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core/Cargo.toml`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/Cargo.toml`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/lmstudio/Cargo.toml`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/.config/nextest.toml` | bounded package-identity slice only: package target is now `ontocode-core`, `codex-core` remains as a temporary workspace/dependency alias to avoid import churn, and nextest package filters now target `ontocode-core` | `cargo metadata --manifest-path ontocode-rs/Cargo.toml --no-deps`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp just test -p ontocode-core default_ontocode_home_falls_back_to_legacy_codex_base_config`, `just fmt`, and `git diff --check` passed; cargo emitted unrelated global-cache disk-full warnings on this host | restore `codex-core` package name and remove alias entries |
| F4-B | Package migration | direct Cargo dependents move from `codex-core` alias to `ontocode-core` | done | manager | direct dependent `Cargo.toml` files in `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs` plus any package-target config that still hard-codes `codex-core` | accepted bounded package-identity follow-up: direct manifest dependency declarations now point at `ontocode-core`, no scoped package-target filter still hard-codes `package(codex-core)`, the temporary root workspace alias remains for compatibility, and docs/comments/runtime strings stayed out of scope; runtime still cannot prove the requested sub-agent model pin | `just fmt`, `cargo metadata --manifest-path Cargo.toml --no-deps`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp just test -p ontocode-core default_ontocode_home_falls_back_to_legacy_codex_base_config`, and `git diff --check` passed | restore dependent package names to `codex-core` and keep alias-only mode |
| F4-C | Package migration | `core-api`, `core-plugins`, and `core-skills` package identity rename with compatibility aliases | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core-api/Cargo.toml`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core-plugins/Cargo.toml`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core-skills/Cargo.toml`; root workspace `Cargo.toml`; direct dependent `Cargo.toml` files | accepted bounded package-identity slice: the three package targets are now `ontocode-*`, temporary `codex-*` workspace aliases remain for compatibility-only manifest wiring, direct dependent manifests moved to `ontocode-*`, and docs/comments/runtime strings stayed out of scope; runtime still cannot prove the requested sub-agent model pin | `just fmt`, `cargo metadata --manifest-path Cargo.toml --no-deps`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp just test -p ontocode-core-api --no-tests pass`, and `git diff --check` passed | restore old package names and remove new alias entries |
| F4-D | Package migration | remove temporary `codex-core*` manifest aliases | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/Cargo.toml`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/lmstudio/Cargo.toml` | accepted bounded manifest cleanup: temporary `codex-core*` workspace alias entries are removed, the last direct-path alias use now depends on `ontocode-core`, and docs/comments/runtime strings remained out of scope | `just fmt`, `cargo metadata --manifest-path ontocode-rs/Cargo.toml --no-deps --format-version=1`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp just test -p ontocode-core-api --no-tests pass`, and `git diff --check` passed | restore alias entries and last direct-path alias use |
| F4-E | Package migration | build/test command surfaces move from `codex-core` to `ontocode-core` | done | manager | build/test config, scripts, and tests in `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs` plus repo tools/docs that specifically instruct `cargo/just test -p codex-core` | accepted bounded command-surface cleanup: active just/test/example command strings and package-target expectations now use `ontocode-core`, general runtime/doc history stayed out of scope, and the requested sub-agent model pin still could not be proven by this runtime | `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp CARGO_BUILD_JOBS=8 just fmt`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp CARGO_BUILD_JOBS=8 just test -p ontocode-core canonicalizes_word_only_shell_scripts_to_inner_command`, `git diff --check`, and `ontoindex analyze --skills --skip-agents-md && rm -rf .ontoindex` passed | restore old package-target command strings |
| F4-F | Package migration | root/tooling tracking command cleanup for stale `codex-core` references | done | manager | root `justfile`, memory-bank tracking text, and tooling/script example surfaces that still present `codex-core` as the active package target | accepted bounded housekeeping slice: root/tooling active command examples and tracking verification strings now use `ontocode-core`; broader prose/history/runtime strings remained out of scope | `git diff --check -- justfile tools/argument-comment-lint/README.md .memory-bank/ONTOCODE_FULL_LEGACY_MIGRATION_TRACKING.md`, `git diff --check -- tools/argument-comment-lint/README.md scripts/test-remote-env.sh`, and sibling OntoIndex refresh passed | restore old command/example strings |
| F4-G | Package migration | sibling docs/tests/snapshots command-example cleanup for stale `codex-core` references | done | manager | scoped docs/tests/snapshots/example-string surfaces in `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs` only | accepted bounded non-runtime cleanup only: stale example/test/snapshot package-target strings in the sibling TUI/core test surfaces now present `ontocode-core` as the active command target; runtime labels, compatibility comments, telemetry names, and prose about historical crate ownership stayed out of scope | `CARGO_BUILD_JOBS=8 just fmt`, `CARGO_BUILD_JOBS=8 just test -p ontocode-tui consecutive_unordered_list_local_file_links_do_not_detach_paths`, `CARGO_BUILD_JOBS=8 just test -p ontocode-tui unified_exec_wait_after_final_agent_message_snapshot`, `CARGO_BUILD_JOBS=8 just test -p ontocode-tui unified_exec_wait_before_streamed_agent_message_snapshot`, `CARGO_BUILD_JOBS=8 just test -p ontocode-tui unified_exec_wait_status_renders_command_in_single_details_row_snapshot`, `CARGO_BUILD_JOBS=8 just test -p ontocode-core canonicalizes_word_only_shell_scripts_to_inner_command`, and `git diff --check` passed | restore prior example/test strings |
| F5 | Env/state migration | config/state/log/cache names | in_progress | manager | config/state/doctor/runtime in clean sibling worktree `/opt/demodb/_workfolder/ontocode-f1-layout` | pre-edit impact run; `Config.load_default_with_cli_overrides_for_codex_home` is CRITICAL, `runtime_migrator` HIGH, `StateRuntime.init_inner`/doctor helpers LOW; stage remains split into additive sub-slices only | config/home-dir, state migration, resume-session, and doctor diagnostics tests pending for later sub-slices | keep old readers |
| F5-A | Env/state migration | additive home alias/read-path + doctor wording | done | Copernicus + manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/utils/home-dir/src/lib.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/doctor.rs` | accepted under narrowed LOW-risk sub-slice: keep `ONTOCODE_HOME` first, preserve legacy `CODEX_HOME` fallback/readers, and shift doctor wording to Ontocode-first compatibility language only | `just fmt`, `cargo test -p codex-utils-home-dir`, `cargo test -p codex-cli --bin ontocode doctor::tests::resolved_home_detail_uses_ontocode_label -- --exact`, `cargo test -p codex-cli --bin ontocode doctor::tests::config_check_reports_ontocode_home_and_legacy_compatibility -- --exact`, and `git diff --check` passed | keep old readers and restore prior doctor copy |
| F5-B | Env/state migration | additive home-resolution diagnostics outside doctor | done | Rawls + manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/app-server-daemon/src/lib.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/app-server-transport/src/transport/mod.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/mcp_cmd.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/plugin_cmd.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/marketplace_cmd.rs` | manager preflight kept logic frozen; accepted copy-only Ontocode-first/legacy-compatible resolution messages in existing call sites | `cargo test -p codex-cli --bin ontocode --no-run`, `cargo test -p ontocode-app-server-transport --lib --no-run`, and `git diff --check` passed | keep old readers and revert copy-only edits |
| F5-C | Env/state migration | standalone logs utility `ONTOCODE_HOME` compatibility | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/state/src/bin/logs_client.rs` | manager preflight isolates this to the standalone logs utility; `logs_db_path` impact is LOW while `runtime_db_paths` remains CRITICAL and out of scope | `just fmt`, `just test -p codex-state`, and `git diff --check` passed | preserve `CODEX_HOME` fallback and restore prior CLI copy |
| F5-D | Env/state migration | doctor display labels and rollout-remedy copy | done | Hooke + manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/doctor/output/detail.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/doctor/output.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/doctor/thread_inventory.rs` | manager preflight kept this in doctor display/remedy surfaces only; `parity_check_from_scan_and_rows`, `state_check`, and `config_check` stayed LOW, and `runtime_db_paths` remained out of scope | `just fmt`, `just test -p codex-cli render_human_report_uses_ontocode_home_labels`, `just test -p codex-cli missing_state_db_check_uses_ontocode_home_remedy_for_scan_failures`, and `git diff --check` passed | restore prior doctor labels/remedy copy |
| F5-E | Env/state migration | docs/comments and help-text wording for home paths | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/app-server-daemon/README.md`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/lib.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/main.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/sandbox_setup.rs` | manager preflight kept this slice copy-only in README/doc-comment/help-text surfaces; `parse_allow_unix_socket_path` and `parse_setup_command` stayed LOW-risk and no config/runtime/state logic moved | `just fmt` and `git diff --check` passed | restore prior wording |
| F5-F | Env/state migration | additive `ONTOCODE_SQLITE_HOME` config resolver support | done | Turing + manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core/src/config/mod.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core/src/config/config_tests.rs` | manager preflight narrows this to the private sqlite-home env resolver only; upstream blast radius for the public config loader stays CRITICAL, so behavior changes remained additive and test-backed | `just fmt`, `just test -p ontocode-core resolve_sqlite_home_env_prefers_ontocode_sqlite_home`, `just test -p ontocode-core resolve_sqlite_home_env_falls_back_to_legacy_codex_sqlite_home`, and `git diff --check` passed | preserve `CODEX_SQLITE_HOME` fallback and restore prior resolver behavior |
| F5-G | Env/state migration | runtime DB path compatibility for Ontocode home/state | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/state/src/runtime.rs` | manager preflight kept this additive and read-only for inspection/repair surfaces: primary runtime DB paths stay under the active sqlite home, and sibling legacy `.codex` paths are appended only when the active sqlite home is `.ontocode` | `just fmt`, `just test -p codex-state runtime_db_paths_include_legacy_codex_fallback_for_default_ontocode_home`, `just test -p codex-state runtime_db_paths_do_not_add_legacy_fallback_for_custom_home`, and `git diff --check` passed | preserve primary path order and remove legacy sibling fallback entries |
| F5-H | Env/state migration | legacy config read fallback for default Ontocode home | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core/src/config/mod.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/core/src/config/config_loader_tests.rs` | manager preflight kept this additive and read-only in the config loader path; `.ontocode` stays authoritative, sibling `.codex` is consulted only when the active default-home user config is absent, and profile fallback wires the existing selected-config path instead of changing write targets | `just fmt`, `just test -p ontocode-core default_ontocode_home_falls_back_to_legacy_codex_base_config`, `just test -p ontocode-core default_ontocode_home_keeps_active_base_config_precedence_over_legacy_codex`, `just test -p ontocode-core default_ontocode_home_profile_falls_back_to_legacy_codex_home`, and `git diff --check` passed | preserve active `.ontocode` precedence and remove legacy fallback branch |
| F5-I | Env/state migration | additive doctor visibility for legacy sibling rollout/session roots | done | manager | `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/doctor.rs`; `/opt/demodb/_workfolder/ontocode-f1-layout/ontocode-rs/cli/src/doctor/thread_inventory.rs` | accepted bounded diagnostics/read-path slice only: when the active home is `.ontocode`, doctor now reports legacy sibling rollout-home details and also scans sibling legacy `.codex` session roots for rollout parity visibility; runtime writes, state DB writes, and resume behavior stayed out of scope | `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp CARGO_BUILD_JOBS=8 just fmt`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp CARGO_BUILD_JOBS=8 just test -p codex-cli --bin ontocode rollout_stats_details_include_legacy_sibling_home_counts`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp CARGO_BUILD_JOBS=8 just test -p codex-cli --bin ontocode thread_inventory_check_scans_legacy_sibling_rollouts_for_default_ontocode_home`, `TMPDIR=/opt/demodb/_workfolder/ontocode-f1-layout/tmp CARGO_BUILD_JOBS=8 just test -p codex-cli --bin ontocode thread_inventory_check_ok_when_rollouts_match_db`, and `git diff --check -- cli/src/doctor.rs cli/src/doctor/thread_inventory.rs` passed | preserve active-root precedence and remove legacy scan fallback |
| F5-J | Main checkout reconciliation | apply accepted runtime DB path compatibility to `/opt/demodb/_workfolder/ontocode` | done | manager | `/opt/demodb/_workfolder/ontocode/ontocode-rs/state/src/runtime.rs`; tracking file | OntoIndex fresh; `runtime_db_paths` impact HIGH/DANGEROUS due public API and direct doctor/recovery callers, so scope stayed additive read-path compatibility only; sub-agent model pin cannot be enforced by this runtime, so senior handled locally | `TMPDIR=/opt/demodb/_workfolder/ontocode/tmp CARGO_BUILD_JOBS=8 just fmt`, `TMPDIR=/opt/demodb/_workfolder/ontocode/tmp CARGO_BUILD_JOBS=8 just test -p ontocode-state runtime_db_paths_include_legacy_codex_fallback_for_default_ontocode_home`, `TMPDIR=/opt/demodb/_workfolder/ontocode/tmp CARGO_BUILD_JOBS=8 just test -p ontocode-state runtime_db_paths_do_not_add_legacy_fallback_for_custom_home`, and `TMPDIR=/opt/demodb/_workfolder/ontocode/tmp CARGO_BUILD_JOBS=8 just test -p ontocode-state` passed | remove appended legacy sibling fallback entries and tests |
| F5-K | Main checkout reconciliation | apply accepted legacy config read fallback to `/opt/demodb/_workfolder/ontocode` | done | manager | `/opt/demodb/_workfolder/ontocode/ontocode-rs/core/src/config/mod.rs`; `/opt/demodb/_workfolder/ontocode/ontocode-rs/core/src/config/config_loader_tests.rs`; tracking file | OntoIndex fresh; `load_config_layers_state` and `Config.load_default_with_cli_overrides_for_codex_home` impact CRITICAL, so scope stayed additive default-home read fallback only, with active `.ontocode` precedence and explicit selected-config overrides preserved | `TMPDIR=/opt/demodb/_workfolder/ontocode/tmp CARGO_BUILD_JOBS=8 just fmt`, three focused `just test -p ontocode-core default_ontocode_home_*` tests, `git diff --check`, and scoped `gn_verify_diff` passed; package-wide `TMPDIR=/opt/demodb/_workfolder/ontocode-test-tmp CARGO_BUILD_JOBS=8 just test -p ontocode-core` still fails in unrelated code-mode and compact-remote-parity tests | remove fallback helper and focused tests |
| F5-L | Verification unblock | triage package-wide `ontocode-core` failures outside F5-J/F5-K | in_progress | manager | pending diagnosis | OntoIndex fresh; no code-symbol edit until a failing owner is identified and impact is checked | pending | no code rollback unless a scoped fix is applied |
| F1-M | Main checkout layout reconciliation | move the legacy Rust workspace directory to `/opt/demodb/_workfolder/ontocode/ontocode-rs` and repair active path/build references | done | manager | `ontocode-rs/` tree; root docs/tooling/path references; tracking file | OntoIndex fresh before move and refreshed after acceptance; mechanical path/layout reconciliation of already accepted sibling F1/F2/F3 direction; Cargo metadata now reports workspace root `ontocode-rs` and zero `codex` binary targets | `just fmt`, `cargo metadata --manifest-path ontocode-rs/Cargo.toml --no-deps`, `cargo build -p ontocode-cli --bin ontocode`, helper binary build batch, `just test -p ontocode-cli --test ontocode_alias`, and `git diff --check` passed | restore the old workspace directory name and path references |
| F4-H | Package migration | rename root npm wrapper folder from `codex-cli/` to `ontocode-cli/` while preserving public npm compatibility | done | manager | `codex-cli/`; `ontocode-cli/`; `pnpm-workspace.yaml`; `scripts/stage_npm_packages.py`; root run scripts | OntoIndex fresh; Cargo package identity already `ontocode-cli`, so this slice stayed folder/tooling cleanup only and preserved `@openai/codex` package compatibility | `corepack pnpm list --depth -1`, npm staging smoke, `just fmt`, `cargo build -p ontocode-cli --bin ontocode`, `./target/debug/ontocode --version`, scoped stale-reference scan, and `git diff --check` passed; global `gn_verify_diff` could not prove scope because the existing large folder-move diff overflowed its git buffer | move folder back and restore path references |
| F6 | Protocol/telemetry versioning | wire/generated/analytics names | blocked | TBD | schemas/generated SDK/telemetry | blocked on versioning owner | schema/protocol tests | keep old version |
| F7 | Final legacy removal | compatibility cleanup | blocked | TBD | compatibility aliases | blocked on release adoption evidence | full release smoke | restore aliases |

## Dispatch Log

| Step | Task | Update |
| --- | --- | --- |
| 1 | F0 | Opened tracking and marked Stage 0 manager gate in progress. |
| 2 | F0-A | Dispatched layout/build graph matrix worker with disjoint write scope. |
| 3 | F0-B | Dispatched CLI/helper/runtime matrix worker with disjoint write scope. |
| 4 | F0-C | Dispatched package/SDK/state/protocol/telemetry matrix worker with disjoint write scope. |
| 5 | F0-A | Accepted layout/build graph matrix after manager review and whitespace verification. |
| 6 | F0-B | Accepted CLI/helper/runtime matrix after manager review and whitespace verification. |
| 7 | F0-C | Accepted package/SDK/state/protocol/telemetry matrix after manager review and whitespace verification. |
| 8 | F0-D | Started manager integration review after all three Stage 0 matrix files were accepted and OntoIndex refreshed. |
| 9 | F0-D | Stage 0 review accepted matrices and blocked F1-F7 implementation pending clean/checkpointed worktree plus release/versioning owners. |
| 10 | F0-E | Started senior unblock pass after renewed manager instruction; current worktree classification shows 1688 modified files, 436 untracked files, and 1 deleted file. |
| 11 | F0-E-A | Dispatching dirty worktree isolation worker with disjoint memory-bank write scope. |
| 12 | F0-E-B | Dispatching F1 path/layout preflight worker with disjoint memory-bank write scope. |
| 13 | F0-E-C | Dispatching release/versioning gate worker with disjoint memory-bank write scope. |
| 14 | F0-E-B | Accepted F1 path/layout preflight; implementation remains blocked until dirty-worktree isolation is accepted. |
| 15 | F0-E-C | Accepted release/versioning owner gate proposal; F4-F6 remain blocked until owners approve the recorded gates. |
| 16 | F0-E-A | Accepted dirty-worktree unblock report; manager decision is to use a clean sibling worktree for F1 instead of moving paths in the dirty checkout. |
| 17 | F1 | Created clean sibling worktree `/opt/demodb/_workfolder/ontocode-f1-layout` on branch `ontocode-f1-layout-2026-06-14`; F1 implementation must run there, not in the dirty main checkout. |
| 18 | F1 | Ran pre-edit OntoIndex batch impact for path-sensitive owners; dispatching F1 layout-only implementation worker in clean sibling worktree. |
| 19 | F1-A | Started bounded manager prerequisite fix for two missing helper `main.rs` files that existing manifests require for `just fmt`. |
| 20 | F1-A | Added missing helper wrapper files in F1 worktree; `just fmt` now passes. |
| 21 | F1 | Accepted F1 layout rename in sibling worktree with current package identity preserved as `codex-cli`; required plan commands using `ontocode-cli` are recorded as Stage 2/package-identity follow-up, not F1. |
| 22 | F2 | Started CLI cutover preflight after F1 acceptance; running exact OntoIndex impact before dispatch. |
| 23 | F2 | Warned on HIGH/CRITICAL pre-edit impact and narrowed dispatch to CLI command behavior only; helper/package/protocol/state changes remain out of scope. |
| 24 | F2 | Accepted CLI cutover after manager added manifest absence test and active Bazel target rename to `//ontocode-rs/cli:ontocode`. |
| 25 | F3 | Started helper cutover preflight; warned on HIGH/CRITICAL impact and narrowed implementation to helper executable names/values with internal field-name quarantine. |
| 26 | F3 | Accepted helper cutover; legacy crate/package names and `CODEX_LINUX_SANDBOX_ARG0` remain quarantined non-scope surfaces. |
| 27 | F4 | Started package migration in preserve-identity mode: keep npm/Python/SDK package names and imports, switch packaged/runtime binary surfaces to `ontocode` only. |
| 28 | F4 | Accepted preserve-identity package cutover: npm package installs `ontocode` only, runtime compatibility APIs resolve `ontocode`, and packaged Windows helpers stage `ontocode-*` only. |
| 48 | F4-A | Started bounded package-identity fix for `core`: rename the package target to `ontocode-core` while preserving a temporary `codex-core` workspace alias so dependency imports do not churn in the same slice. |
| 49 | F4-A | Accepted bounded `core` package-identity rename: the package target is now `ontocode-core`, `lmstudio` and nextest package filters were updated for the new package name, a compatibility alias still preserves `codex-core` dependency wiring, and the targeted `just test -p ontocode-core ...` run passed when `TMPDIR` was moved off the full `/tmp` filesystem. |
| 50 | F4-B | Started dependent-sweep follow-up: replace direct `codex-core` dependency declarations with `ontocode-core` in Cargo manifests and package-target config only, keep the compatibility alias in the workspace for now, and record that sub-agent model pinning still cannot be enforced by runtime. Scope is limited to direct `Cargo.toml` dependents plus package-target config in the clean sibling worktree; docs/comments/runtime strings stay out. |
| 51 | F4-B | Accepted direct-dependent manifest sweep: scoped `Cargo.toml` dependents now use `ontocode-core`, no in-scope package-target filter still names `package(codex-core)`, the temporary root alias remains for compatibility, focused `ontocode-core` test/metadata/diff checks passed, and the sibling worktree index was refreshed after acceptance. |
| 52 | F4-C | Started bounded package-identity follow-up for `core-api`, `core-plugins`, and `core-skills`: rename package targets to `ontocode-*`, preserve temporary compatibility aliases if needed, and keep the slice limited to Cargo/package-target wiring only. |
| 53 | F4-C | Accepted bounded package-identity rename for `core-api`, `core-plugins`, and `core-skills`: package targets are now `ontocode-*`, direct manifest dependents moved to the new names, temporary `codex-*` workspace aliases remain for compatibility-only wiring, focused metadata and renamed-target verification passed, and the sibling worktree index was refreshed after acceptance. |
| 54 | F4-D | Started final manifest-only cleanup for `core*` package aliases: remove temporary `codex-core*` workspace alias entries and the last direct-path alias use now that no manifest depends on them. |
| 55 | F4-D | Accepted final manifest-only cleanup for `core*` package aliases: root workspace alias entries are gone, `lmstudio` now depends on `ontocode-core` directly, focused metadata and renamed-target verification passed, and the sibling worktree index must be refreshed again. |
| 56 | F4-E | Started bounded command-surface cleanup for `codex-core` -> `ontocode-core`: update active build/test package-target references only, not general prose/history/runtime strings. |
| 57 | F4-F | Started root/tooling housekeeping for stale `codex-core` command strings outside the clean worktree package manifests: root `justfile`, tracking verification text, and scoped tooling/script examples only. |
| 58 | F4-F | Accepted root/tooling housekeeping for stale `codex-core` command strings: root `justfile`, argument-comment-lint examples, sibling remote-test command comment, and stale tracking verification lines now point at `ontocode-core`; the remaining `codex-core` set is no longer active manifest/command wiring. |
| 59 | F4-G | Started bounded sibling cleanup for stale `codex-core` example strings in docs/tests/snapshots only; runtime labels, telemetry/service names, compatibility comments, and historical prose remain out of scope. |
| 60 | F4-G | Accepted bounded sibling docs/tests/snapshots cleanup: stale active package-target example strings in TUI markdown tests and chatwidget snapshots now point at `ontocode-core`, focused TUI snapshot checks plus one `ontocode-core` command test passed, and the sibling worktree index was refreshed after acceptance. |
| 61 | F5 | Senior stop point after `F4-G`: the remaining work is no longer package-target/example cleanup. What is left now is persisted-state/runtime migration (`core/src/config/mod.rs`, `state/src/runtime.rs`, rollout/session resume flows) plus F6/F7 release-gated protocol cleanup. Do not dispatch another worker unless the task is an explicit compatibility migration slice with resume/data verification. |
| 62 | F5-I | Started explicit compatibility slice for doctor visibility only: inspect active `.ontocode` plus sibling legacy `.codex` rollout/session roots in doctor inventory/stat reporting when the active home is default-shaped, without changing runtime write targets, state DB ownership, or resume behavior. |
| 63 | F5-I | Accepted explicit doctor-visibility slice after focused verification: doctor state details now report legacy sibling rollout home stats when the active home is `.ontocode`, thread inventory parity also scans sibling legacy `.codex` rollout roots for read-only diagnostics, and runtime write/resume/state migration behavior stayed unchanged. |
| 57 | F4-E | Manager continuing the bounded command-surface slice in the clean sibling worktree after F4-D/F4-C acceptance; scope remains justfile/package-target/script/test command surfaces, with runtime/doc history still out of scope. |
| 58 | F4-E | Accepted bounded command-surface cleanup: `justfile`, focused tests, and in-scope executable example strings now point at `ontocode-core`, focused renamed-target verification passed, and the sibling worktree index was refreshed after acceptance. |
| 30 | F5 | Started Stage 5 preflight in preserve mode; exact impact shows CRITICAL config/home blast radius, so the first dispatch is limited to additive `ONTOCODE_*` alias/read-path work with old `CODEX_*` readers preserved. |
| 30 | F5-A | Dispatched bounded worker for home alias/read-path and doctor wording only; runtime could not enforce the requested sub-agent model choice, so that limitation was recorded. |
| 31 | F5-A | Accepted additive home alias/read-path slice after manager review; empty `ONTOCODE_HOME` now falls back like the public env path, doctor reports Ontocode-first home details while preserving legacy `CODEX_HOME` compatibility, and the sibling worktree index must be refreshed before the next slice. |
| 32 | F5-B | Started preflight for copy-only home-resolution diagnostics outside doctor; state filenames, runtime migration, and destructive alias removal remain out of scope. |
| 33 | F5-B | Accepted copy-only home-resolution diagnostics for daemon/transport/MCP/plugin/marketplace paths after manager verification; logic and readers stayed unchanged, the worker runtime could not guarantee the requested model choice, and the sibling worktree index must be refreshed again. |
| 34 | F5-C | Started bounded standalone-logs utility slice: add `ONTOCODE_HOME` compatibility and Ontocode-first copy in `state/src/bin/logs_client.rs` only; `runtime_db_paths`, SQLite filename changes, rollout/session directory migration, and config-loader behavior remain out of scope. |
| 35 | F5-C | Accepted standalone logs utility compatibility slice after manager verification; `--ontocode-home` is now primary with `--codex-home` alias support, `ONTOCODE_HOME` is preferred over legacy `CODEX_HOME`, and the sibling worktree index must be refreshed before the next task. |
| 36 | F5-D | Started bounded doctor display slice: update remaining human-facing `CODEX_HOME` labels and thread-inventory rollout remedies to Ontocode-first compatibility wording, with snapshot/test updates only; no state-path behavior or runtime DB path logic is in scope. |
| 37 | F5-D | Accepted doctor display/remedy slice after manager verification; doctor output now renders Ontocode-first home labels, rollout remedy text uses legacy-compatible Ontocode wording, and the sibling worktree index was refreshed again after acceptance. |
| 38 | F5-E | Started bounded docs/comments slice: update remaining non-generated home-path wording in app-server-daemon docs and CLI comments/help text only; protocol/schema/generated descriptions stay with F6, and config/runtime/state behavior remains out of scope. |
| 39 | F5 | Senior stop point: low-risk copy-only F5 slices are exhausted. Remaining F5 work is the real persisted-state migration surface (`Config.load_default_with_cli_overrides_for_codex_home`, `runtime_db_paths`, SQLite home/layout, rollout/session compatibility, resume behavior) and should not be dispatched as another wording-only task. |
| 39 | F5-E | Accepted docs/comments/help-text slice after manager verification; app-server daemon docs and remaining CLI home-path comments now use Ontocode-first legacy-compatible wording without changing flags, config semantics, or runtime behavior. |
| 40 | F5-F | Started bounded real-config slice: add additive `ONTOCODE_SQLITE_HOME` support in the private sqlite-home env resolver only, keep `CODEX_SQLITE_HOME` fallback, and prove behavior with direct resolver/config tests instead of broader runtime migration. |
| 41 | F5-F | Accepted bounded sqlite-home resolver slice after manager fix/verification; whitespace-only `ONTOCODE_SQLITE_HOME` no longer blocks the legacy `CODEX_SQLITE_HOME` fallback, both direct resolver tests passed, and the sibling worktree index must be refreshed before any next task. |
| 42 | F5 | Senior stop point after `F5-F`: remaining Stage 5 work is no longer additive alias coverage. It is the persisted-state/runtime migration surface (`Config.load_default_with_cli_overrides_for_codex_home`, `runtime_db_paths`, rollout/session compatibility, resume behavior) and requires a dedicated migration task with explicit compatibility verification. |
| 43 | F5-G | Started dedicated persisted-state slice: inspect `runtime_db_paths` and related recovery/doctor callers for additive Ontocode-home compatibility only; keep legacy paths readable, require focused tests, and record that sub-agent model pin cannot be enforced by runtime. |
| 44 | F5-G | Accepted additive runtime DB path compatibility in `codex-state`; `runtime_db_paths` now appends sibling legacy `.codex` database paths only for `.ontocode` homes, primary runtime write paths remain unchanged, focused state-crate tests passed, and the sibling worktree index must be refreshed again. |
| 45 | F5-H | Started dedicated config-compatibility slice: inspect `Config.load_default_with_cli_overrides_for_codex_home` for additive legacy `.codex/config.toml` read fallback only when the active default home is `.ontocode` and no active config exists; keep `.ontocode` precedence, require focused config tests, and record that sub-agent model pin cannot be enforced by runtime. |
| 46 | F5-H | Accepted additive legacy config read fallback in `codex-core`; default `.ontocode` homes now read sibling legacy `.codex` user config only when the active base/profile config is absent, active `.ontocode` config keeps precedence, focused config-loader tests passed, the runtime could not enforce the requested sub-agent model pin, and the sibling worktree index must be refreshed again. |
| 47 | F5 | Senior stop point after `F5-H`: additive read-path compatibility is largely exhausted. Remaining Stage 5 work is the true write-path and resume/session migration surface, which now requires an explicit migration strategy and broader compatibility verification rather than another bounded alias fallback. |
| 48 | F5-J | Started main-checkout reconciliation for accepted F5-G behavior: add sibling legacy `.codex` runtime DB paths only to `runtime_db_paths` inspection/repair output when the active home is default-shaped `.ontocode`; primary runtime write paths remain unchanged. |
| 49 | F5-J | Accepted main-checkout reconciliation: `runtime_db_paths` in `/opt/demodb/_workfolder/ontocode` now appends sibling legacy `.codex` database paths only for default-shaped `.ontocode` homes, custom homes stay unchanged, and `ontocode-state` tests passed. |
| 50 | F5-K | Started main-checkout reconciliation for accepted F5-H behavior: read sibling legacy `.codex/config.toml` only when active default `.ontocode` user/profile config is absent; no write-path or migration-window closure changes are in scope. |
| 51 | F5-K | Accepted main-checkout reconciliation: default `.ontocode` config loading now falls back to sibling legacy `.codex` only when active user/profile config is absent, focused fallback/preference/profile tests passed, and the package-wide core run is blocked by unrelated code-mode and compact-remote-parity failures. |
| 52 | F5-L | Started package-wide verification blocker triage after F5-K: isolate code-mode and compact-remote-parity failures using focused reruns before considering any implementation change. |
| 53 | F1-M | Started main-checkout layout reconciliation after manager/user direction changed: move the legacy Rust workspace folder itself to `ontocode-rs/`, keep the change mechanical, and verify with metadata/build-path checks before resuming F5-L. |
| 54 | F1-M | Accepted main-checkout layout reconciliation: the Rust workspace now lives at `ontocode-rs/`, active build/test/path references were repaired, Cargo metadata reports zero `codex` binary targets, focused CLI alias tests passed, and `git diff --check` passed. |
| 55 | F4-H | Started root npm wrapper folder cleanup: rename `codex-cli/` to `ontocode-cli/`, update workspace/release script path references, and keep published package compatibility unchanged. |
| 56 | F4-H | Accepted root npm wrapper folder cleanup: root `codex-cli/` no longer exists, pnpm workspace and staging script load `ontocode-cli/`, stale local `codex-cli`/`--bin codex` command references were removed from root tooling, public npm package names were intentionally preserved for compatibility, and OntoIndex must be refreshed after acceptance. |

## Current Blockers

- F5 remains constrained to additive alias/read-path work because config/home resolution is CRITICAL blast radius; no destructive state-path removal or migration-window closure is allowed in this slice.
- Remaining F5 work still excludes SQLite filename changes, rollout/session directory migration, destructive env alias removal, and state-runtime path rewrites until a later bounded sub-slice is reviewed.
- The remaining F5 surface is now dominated by CRITICAL/HIGH owners (`Config.load_default_with_cli_overrides_for_codex_home`, `runtime_db_paths`, and persisted rollout/session compatibility). Further progress requires a dedicated migration slice with explicit resume/data-compatibility verification, not more copy-only dispatches.
- F6 and F7 remain blocked on release/versioning and adoption gates even after the accepted F5 additive slices.
- Current worktree has broad unrelated changes; every worker must keep scope disjoint and avoid reverting unrelated files.
