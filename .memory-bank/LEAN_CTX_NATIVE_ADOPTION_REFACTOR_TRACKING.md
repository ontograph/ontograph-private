---
name: Lean-ctx Native Adoption Refactor Tracking
type: tracking
source_plan: LEAN_CTX_NATIVE_ADOPTION_REFACTOR_PROJECT_PLAN.md
date: 2026-06-28
status: active
---

# Lean-ctx Native Adoption Refactor Tracking

Source plan: [LEAN_CTX_NATIVE_ADOPTION_REFACTOR_PROJECT_PLAN.md](LEAN_CTX_NATIVE_ADOPTION_REFACTOR_PROJECT_PLAN.md)

## Manager State

- Last decision: `no_eligible_open_task` for implementation dispatch after the package-bundle inclusion slice; closure is working-tree-only until the untracked fork/tracker files are intentionally added and committed.
- Active next task: `none`
- OntoIndex state: fresh at HEAD `5edde24a78efe0f10bc710936dfa228427ab7fd1`; dirty worktree and missing embeddings degrade confidence for repo-wide freshness only
- Authority note: OntoIndex routes the existing plugin/MCP owners, but direct source is authoritative for `third_party/lean-ctx-fork`; the fork files are untracked and not indexed, so OntoIndex diff verification cannot prove fork symbol scope.
- Durability state: `git status --short` still reports the tracker and all touched fork files as untracked (`??`). Treat this as validated working-tree evidence, not durable repository history, until those files are added and committed.
- Scoped verification: `gn_verify_diff` passed with explicit changed-file and changed-symbol overrides for the fork slice after the final tracker update. Repo-wide diff verification remains unsuitable while unrelated dirty files and untracked fork files are present.

## Current Source Evidence

- Plugin contract is bounded to HTTP MCP plus `ctx_read`, `ctx_search`, and `ctx_summary` in [plugins/ontocode-lean-ctx/.mcp.json](/opt/demodb/_workfolder/ontocode/plugins/ontocode-lean-ctx/.mcp.json:1) and [plugins/ontocode-lean-ctx/README.md](/opt/demodb/_workfolder/ontocode/plugins/ontocode-lean-ctx/README.md:20).
- The fork already has `ToolProfile::Ontocode` and profile parsing in [third_party/lean-ctx-fork/rust/src/core/tool_profiles.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/core/tool_profiles.rs:15).
- Invoker suppression and dispatch allowlist enforcement for the Ontocode profile already exist in [third_party/lean-ctx-fork/rust/src/server/tool_visibility.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/server/tool_visibility.rs:173).
- The registry keeps the broad `build_registry()` source of truth, but Ontocode runtime startup now uses the profile-aware `build_registry_for_profile(...)` path in [third_party/lean-ctx-fork/rust/src/server/registry.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/server/registry.rs:119) and [third_party/lean-ctx-fork/rust/src/tools/server_lifecycle.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/tools/server_lifecycle.rs:179).
- Ontocode startup now skips inherited product/network/user-plugin side effects: proxy autostart, background publish, rules injection, hook refresh, version checks, savings autopush, plugin registry discovery, and session start/end hooks. Local storage maintenance and HTTP index prewarm remain in place.
- `/v1/capabilities` now reports runtime tools, `server.tool_profile`, and maintained-fork provenance; [third_party/lean-ctx-fork/docs/contracts/capabilities-contract-v1.md](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/docs/contracts/capabilities-contract-v1.md:27) documents the current server payload shape.
- Primary Ontocode/Codex package bundles can now carry a prebuilt maintained backend as `codex-resources/lean-ctx[.exe]`, with `codex-package.json` recording the resource as `leanCtxBackend`. The stable builder flag is `--lean-ctx-bin`; the archive wrapper auto-detects a colocated `lean-ctx[.exe]` for the `primary` bundle only.
- Broad default features still exist in [third_party/lean-ctx-fork/rust/Cargo.toml](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/Cargo.toml:69).

## Ledger

| ID | Task | Status | Owner | Notes |
| --- | --- | --- | --- | --- |
| LCTX-REF-0 | Baseline inventory and guard tests | completed | manager | Completed from direct source plus validation evidence. Existing plugin contract remains bounded to `ctx_read`, `ctx_search`, and `ctx_summary`; the negative dispatch proof already exists in `third_party/lean-ctx-fork/rust/src/tools/mod.rs`; repo-owned smoke passed. Validation: `bash -n scripts/run_lean_ctx_plugin_backend.sh scripts/smoke_lean_ctx_plugin_backend.sh`, `cd third_party/lean-ctx-fork/rust && CARGO_BUILD_JOBS=8 cargo test ontocode_profile_blocks_noncarried_tool_dispatch`, and `CARGO_BUILD_JOBS=8 just lean-ctx-plugin-backend-smoke`. Side-effect owners pinned in `cli/dispatch/server.rs`, `server/server_handler.rs`, and `http_server/mod.rs`. |
| LCTX-REF-1 | Native Ontocode backend mode API evidence | completed-no-code | manager | Closed as no-code. `ToolProfile::Ontocode`, `tool_profile_effective()`, invoker suppression, and `profile_allows_dispatch(...)` already provide the reusable mode query needed by later slices. No extra abstraction is justified yet. |
| LCTX-REF-2 | Ontocode-mode registry slimming | completed | implementation-worker | Completed with a runtime-only registry path. `build_registry()` remains the full-registry source of truth, while [third_party/lean-ctx-fork/rust/src/server/registry.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/server/registry.rs:119) now adds `build_ontocode_registry()` plus `build_registry_for_profile(...)`, and [third_party/lean-ctx-fork/rust/src/tools/server_lifecycle.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/tools/server_lifecycle.rs:179) now wires `LeanCtxServer` startup through the profile-aware runtime builder. Validation: `cd third_party/lean-ctx-fork/rust && CARGO_BUILD_JOBS=8 cargo test --lib ontocode_profile -- --nocapture`, `cd /opt/demodb/_workfolder/ontocode && CARGO_BUILD_JOBS=8 just lean-ctx-plugin-backend-smoke`, and `gn_verify_diff` PASS for the two changed files. OntoIndex limitation remains: third-party fork symbols were not indexed, so direct source stayed authoritative for exact owners. |
| LCTX-REF-3 | External startup side-effect gates | completed | implementation-worker | Completed with the follow-up review fix. [third_party/lean-ctx-fork/rust/src/cli/dispatch/server.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/cli/dispatch/server.rs:78) now skips proxy autostart, background publish, plugin registry discovery, `OnSessionStart`, and `OnSessionEnd` hooks in Ontocode mode. [third_party/lean-ctx-fork/rust/src/server/server_handler.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/server/server_handler.rs:171) skips rules injection, hook refresh, and background version checks in Ontocode mode while keeping local storage maintenance. [third_party/lean-ctx-fork/rust/src/http_server/mod.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/http_server/mod.rs:1049) now skips plugin registry discovery, savings autopush, and session-end hooks in Ontocode HTTP/IPC mode while keeping index prewarm. Validation: `cargo fmt`, `cd third_party/lean-ctx-fork/rust && CARGO_BUILD_JOBS=8 cargo test --lib ontocode_profile -- --nocapture`, `cd /opt/demodb/_workfolder/ontocode && CARGO_BUILD_JOBS=8 just lean-ctx-plugin-backend-smoke`, and no trailing-whitespace hits from `rg -n "[ \t]+$"` on the touched code/doc/tracking files. |
| LCTX-REF-4 | Unsupported CLI command gates | gated | manager | Open only if `LCTX-REF-0` proves these commands are reachable from the supported Ontocode backend path. |
| LCTX-REF-5 | Feature/dependency slimming | gated | manager | Open only after measured build, runtime, or review cost remains material after the registry/startup refactors. Current evidence only proves broad default features still exist; it does not prove they are costly enough to dispatch this slice. |
| LCTX-REF-6 | Backend capability/provenance output | completed | implementation-worker | Completed and contract drift fixed. [third_party/lean-ctx-fork/rust/src/core/server_capabilities.rs](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/rust/src/core/server_capabilities.rs:33) reports runtime tools from `build_registry_for_profile(...)`, `server.tool_profile`, and maintained-fork provenance (`kind=maintained-fork`, `source_root=third_party/lean-ctx-fork`). [third_party/lean-ctx-fork/docs/contracts/capabilities-contract-v1.md](/opt/demodb/_workfolder/ontocode/third_party/lean-ctx-fork/docs/contracts/capabilities-contract-v1.md:27) now documents the server fields and example shape. Validation: `cd third_party/lean-ctx-fork/rust && CARGO_BUILD_JOBS=8 cargo test --lib ontocode_profile -- --nocapture`, `cd third_party/lean-ctx-fork/rust && CARGO_BUILD_JOBS=8 cargo test --test capabilities_contract_up_to_date`, `cd /opt/demodb/_workfolder/ontocode && CARGO_BUILD_JOBS=8 just lean-ctx-plugin-backend-smoke`, and no trailing-whitespace hits from `rg -n "[ \t]+$"` on the touched code/doc/tracking files. |
| LCTX-REF-7 | Extraction/native replacement decision | gated | manager | ADR or explicit no-dispatch only after prior slices land or fail with evidence. |
| LCTX-BUNDLE-1 | Include maintained lean-ctx backend in Ontocode package bundle | completed | implementation-worker | Completed in the existing package builder without adding a second bundler. `scripts/build_codex_package.py` now accepts `--lean-ctx-bin`; package layout copies the executable to `codex-resources/lean-ctx[.exe]`, records `leanCtxBackend`, and validates that recorded resource. `.github/scripts/build-codex-package-archive.sh` forwards an explicit backend path and auto-detects a colocated backend for `primary` bundles only. The full CLI path also fixes the existing Windows helper argparse name mismatch so explicit helper-resource builds run end to end. Validation: package CLI/layout/unit tests, archive script syntax check, CLI help check, manual package smoke with fake Windows resources, and focused OntoIndex diff verification. Release workflows do not build/sign a new lean-ctx binary automatically; that remains a release-infrastructure gate. |

## Stop Gates

- Do not open `LCTX-REF-4` without proof that unsupported product commands are reachable from the supported Ontocode backend operator path.
- Do not open `LCTX-REF-5` without measured build, runtime, or review cost after the earlier refactors; existence of broad default features alone is not enough.
- Do not rewrite this tracking file again without new source, code, test, or validation evidence.
- Do not claim release artifacts always include `lean-ctx` until release CI explicitly builds/signs or otherwise supplies the backend binary to the package wrapper.
- Scope verification caveat: `gn_verify_diff` is expected to fail repo-wide while the worktree contains unrelated dirty files and the fork files remain untracked/outside the OntoIndex graph. Use the listed focused tests plus direct source review as the authority for this fork slice.
- Durability caveat: do not cite this tracker as committed project state until the untracked fork/tracker files are intentionally added and committed.
