name: Claude Code Donor Parked Rows Pre-Junior Project Plan
desc: Junior-safe triage plan for parked Claude Code donor rows from ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW_DEFERRED_NARROW_REJECT.md
type: project_plan
date: 2026-06-16
status: challenged

# Claude Code Donor Parked Rows Pre-Junior Project Plan

## Goal

Use [ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW_DEFERRED_NARROW_REJECT.md](ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW_DEFERRED_NARROW_REJECT.md) as a parking-lot review queue.

This is not an implementation plan for the 146 parked rows. A pre-junior may only verify one row, prove whether it should stay parked, or prepare a senior-ready promotion packet. No Rust code is changed from this plan.

## Challenge Review

- This plan is a brake, not a backlog. It must reduce accidental scope, not create tasks.
- Pre-juniors cannot change row verdicts, move rows between ADRs, or edit the active `KEEP` ADR.
- A promotion packet is only a handoff note for senior review. It does not authorize implementation.
- If a row overlaps Gemini or Oh My Pi plans, leave it parked and link the duplicate. Do not consolidate by copying tasks.
- If OntoIndex is unavailable, the row is blocked. Do not replace the owner check with grep or guesswork.
- OntoIndex freshness is required. If `indexedCommit` differs from `currentCommit`, stop and coordinate before using the plan.

## OntoIndex Owner Challenge

Fresh OntoIndex evidence reinforces that this plan must stay triage-only:

- `ontocode-rs/codex-mcp/src/connection_manager.rs` is already the MCP owner for tool visibility, approval policy, permissions, server/resource lists, resource reads, and tool calls. It is 823 lines, so MCP rows cannot become new browser/debugger/resource surfaces.
- `ontocode-rs/core/src/session/turn.rs` owns turn execution, prompt building, tool construction, compaction, and context updates. It is 2252 lines, so context/cache rows cannot become new prompt-cache or speculative-context behavior.
- `ontocode-rs/hooks/src/engine/discovery.rs` owns hook discovery. It is 1087 lines, so hook rows cannot become a second hook registry or policy layer.
- `ontocode-rs/state/src/runtime/agent_jobs.rs` owns job creation, item state, cancellation, results, and progress. It is 685 lines, so agent rows cannot become scheduler, session-command, or persisted-state work.

Challenge outcome: a pre-junior may produce evidence only. Promotion requires a senior to move the row to an accepted ADR or a separate tracked plan.

## Allowed Work

- Pick exactly one parked row.
- Confirm its current verdict: `NARROW`, `DEFER`, or `REJECT`.
- Check whether the same idea is already covered by Gemini or Oh My Pi donor plans.
- Use OntoIndex to identify the existing owner module.
- Write a short promotion packet only if the row can be reduced to one existing-owner test gap.

## Non-Goals

- Do not implement parked rows directly.
- Do not add runtime state, context fragments, tool registries, MCP browsers, hook registries, scheduler behavior, UI surfaces, plugin systems, or eval frameworks.
- Do not reopen `REJECT` rows unless a senior first changes the ADR verdict.
- Do not create a new ADR for every row. One row needs one concise note.
- Do not edit verdicts, row counts, or parked-row tables from this plan.

## Stage 0: Preflight

Read:

- [ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW_DEFERRED_NARROW_REJECT.md](ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW_DEFERRED_NARROW_REJECT.md)
- [ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW.md](ADR_CLAUDE_CODE_DONOR_CORE_EXTENSION_REVIEW.md)
- [GEMINI_CLI_DONOR_CONTEXT_TOOLS_AGENTS_EVALS_PRE_JUNIOR_PROJECT_PLAN.md](GEMINI_CLI_DONOR_CONTEXT_TOOLS_AGENTS_EVALS_PRE_JUNIOR_PROJECT_PLAN.md)
- [OH_MY_PI_DONOR_KEEP_REFACTOR_MAP_PRE_JUNIOR_PROJECT_PLAN.md](OH_MY_PI_DONOR_KEEP_REFACTOR_MAP_PRE_JUNIOR_PROJECT_PLAN.md)

Checks:

- Verify the parked table still has 146 rows.
- Pick one row only.
- Record the row ID, verdict, category, and claimed owner.

Acceptance:

- No code changed.
- The task note names one row and one existing owner area.
- The task note quotes the parked ADR row text or links the row ID.

## Stage 1: Duplicate Gate

Task:

- Search the Gemini and Oh My Pi pre-junior plans for the same behavior.
- If the idea is already covered there, leave the Claude row parked.
- If the idea conflicts with a blocked scope there, leave the Claude row parked.

Acceptance:

- The note says either `duplicate`, `blocked`, or `not duplicated`.
- Duplicates are not promoted.
- A duplicate row gets no promotion packet.

## Stage 2: OntoIndex Owner Check

Use OntoIndex on the relevant existing owner before proposing any promotion:

- MCP/resource/debug rows: `ontocode-rs/codex-mcp/src/connection_manager.rs`
- Context/cache/speculation rows: `ontocode-rs/core/src/session/turn.rs`, `ontocode-rs/core/src/compact.rs`, or `ontocode-rs/core/src/session/turn_context.rs`
- Hook rows: `ontocode-rs/hooks/src/engine/discovery.rs` or `ontocode-rs/core/src/hook_runtime.rs`
- Agent/job/session rows: `ontocode-rs/state/src/runtime/agent_jobs.rs`
- Code-search rows: keep rejected; OntoIndex is the owner.

Acceptance:

- The note includes the OntoIndex owner file.
- The note includes one OntoIndex fact, such as the owner file public API or module role.
- For hot owners, the note includes the OntoIndex line count and explains why no direct edit is proposed.
- If OntoIndex shows the owner already covers the behavior, the row stays parked.
- If OntoIndex is locked or unavailable, stop; do not substitute manual guessing.

## Stage 3: Narrowing Packet

Only for `NARROW` rows.

Write a concise packet with:

- row ID and current verdict
- exact existing owner file
- one missing behavior
- one smallest failing test that would prove the gap
- why this is not a new runtime concept
- duplicate check result

Packet format:

```md
Row:
Current verdict:
Duplicate gate:
OntoIndex owner:
Missing behavior:
Smallest failing test:
Why this is not new architecture:
Hot-owner risk:
Senior decision needed:
```

Acceptance:

- The packet fits in one short section.
- It does not propose production code.
- It does not introduce new architecture.
- It does not name an implementation worker.
- It is written as a handoff note, not committed into an ADR unless a senior asks.

## Stage 4: Deferred Row Review

Only for `DEFER` rows.

A deferred row can move forward only with fresh evidence:

- a real bug,
- a user-facing regression,
- a security/safety issue,
- or a senior-approved product requirement.

Acceptance:

- Without fresh evidence, leave the row parked.
- With evidence, write a promotion packet and stop for senior review.
- Evidence must be specific; "nice to have" is not enough.

## Stage 5: Rejected Row Review

Only for `REJECT` rows.

Rejected rows stay rejected by default.

Current hard rejects:

- 121: duplicate code exploration
- 124: raw code exposure
- 125: duplicate symbol search
- 126: duplicate file search
- 127: grep wrapper

Acceptance:

- Do not promote these from this plan.
- Pre-juniors may only confirm why the row is rejected.
- If a senior changes one verdict, start again at Stage 0.

## Closure Checklist

- Row count remains 146 unless a senior explicitly moves a row out.
- Any doc edit links back to the parked ADR.
- No Rust files changed.
- `git diff --check` passes for edited markdown.

## Dispatch Ledger

| Date | Row | Verdict | Owner check | Status | Agent model | Notes |
| --- | ---: | --- | --- | --- | --- | --- |
| 2026-06-20 | 002 | NARROW | `ontocode-rs/core/src/tools/spec_plan.rs` | parked | `gpt-5.4-mini` fallback | Stays parked. ADR row 002 asks for a preset abstraction while donor evidence says only `default` exists; no concrete failing test gap was found. OntoIndex reports `spec_plan.rs` owns `build_tool_router`, `search_tool_enabled`, and `tool_suggest_enabled`, is 996 lines, and `build_tool_router` impact is LOW via `ToolRouter::from_turn_context`. Duplicate gate found no dispatchable Gemini/Oh My Pi overlap. Parked ADR count remains 146. Worker was stopped after timeout without changes. |
| 2026-06-20 | 004 | NARROW | `ontocode-rs/core/src/tools/spec_plan.rs` | parked | `gpt-5.4-mini` fallback | Stays parked. Duplicate gate found no exact Gemini/Oh My Pi match, but active Claude KEEP row 010 already owns explicit disabled-tool reasons in the current tool exposure path. OntoIndex reports `spec_plan.rs` owns `build_tool_router`, `search_tool_enabled`, and `tool_suggest_enabled`; `build_tool_router` impact is LOW via `ToolRouter::from_turn_context`. No standalone promotion packet. |
| 2026-06-20 | 007 | NARROW | `ontocode-rs/protocol/src/tool_name.rs` | parked | `gpt-5.4-mini` fallback | Stays parked. Duplicate gate clear, but OntoIndex shows `tool_name.rs` is a 77-line value type with public `new`, `plain`, and `namespaced`; registry-bound alias resolution is not reducible to one owner-local test without architecture creep. `ToolName::namespaced` has CRITICAL blast radius, and existing registry tests already cover explicit plain/namespaced lookup plus spawn-agent matcher aliasing. No promotion packet. |
| 2026-06-20 | 008 | NARROW | `ontocode-rs/core/src/tools/spec_plan.rs`; context attribution owners | parked | `gpt-5.4-mini` fallback | Stays parked. Parked text asks for source attribution, already owned by active Claude KEEP row 072. Donor source asks for deny-rule filtering in `spec_plan.rs`, but no existing config deny surface was found to reduce to one owner-local failing test. OntoIndex reports `spec_plan.rs` is a 996-line hot owner with `build_tool_router`, `search_tool_enabled`, and `tool_suggest_enabled`; `build_tool_router` impact is LOW via `ToolRouter::from_turn_context`. No promotion packet. |
| 2026-06-20 | 013 | DEFER | `ontocode-rs/tui/src/text_formatting.rs`; `ontocode-rs/tui/src/history_cell/mcp.rs` | parked | `gpt-5.4-mini` fallback | Stays parked. No fresh bug/regression/security/product evidence. Donor source is TUI tool-result rendering, while parked text is preference presets; both remain non-core/deferred. OntoIndex shows `format_and_truncate_tool_result` compact-formats/truncates tool results and is called by MCP history-cell rendering. No promotion packet. |
| 2026-06-20 | 015 | NARROW | `ontocode-rs/core/src/tools/spec_plan.rs`; protocol/UI notebook note | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says notebook-style protocol/UI surface, while donor source says embedded-search capability detection. Notebook scope conflicts with Oh My Pi's no-notebook rule; search-capability scope duplicates active Claude KEEP row 006 and Oh My Pi row 11 native search fallback tracking. OntoIndex reports `spec_plan.rs` owns `build_tool_router`, `search_tool_enabled`, and `tool_suggest_enabled`, is 996 lines, and `search_tool_enabled` has HIGH upstream impact across 13 nodes / 4 modules. Existing tests already gate `tool_search` on model search capability and deferred tools, so no single owner-local failing test gap was found. No promotion packet. |
| 2026-06-20 | 016 | DEFER | `ontocode-rs/core/src/shell.rs`; `ontocode-rs/exec-server` Windows shell capability notes | parked | `gpt-5.4-mini` fallback | Stays parked. No fresh bug/regression/security/product evidence was found for the DEFER row. Donor source asks for PowerShell tool exposure behind shell capability detection, but OntoIndex reports `shell.rs` already owns `derive_exec_args`, `get_powershell_shell`, `get_shell`, and `default_user_shell`; the file is 410 lines. Existing code detects `pwsh`/`powershell`, defaults to PowerShell on Windows before `cmd.exe`, and has PowerShell shell tests. `get_powershell_shell` impact is HIGH across 3 modules via `get_shell`, session construction, and shell handlers. No promotion packet. |
| 2026-06-20 | 017 | DEFER | `ontocode-rs/core/src/tools/spec_plan.rs`; shell/notebook mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says OS shell adapter polish needs failures; donor source says notebook-edit capability as a separately gated tool. The sources do not describe one clean gap. Oh My Pi explicitly blocks notebook execution and related runtime surfaces, and no fresh bug/regression/security/product evidence was found. OntoIndex reports `spec_plan.rs` owns `build_tool_router`, `search_tool_enabled`, and `tool_suggest_enabled`; notebook search found Python SDK notebook smoke tests and existing code-mode cell runtime tests, not an approved notebook-edit tool owner. No promotion packet. |
| 2026-06-20 | 018 | DEFER | `ontocode-rs/protocol`; `ontocode-rs/core/src/tools`; MCP/tool-result structured plumbing | parked | `gpt-5.4-mini` fallback | Stays parked. No fresh bug/regression/security/product evidence was found for the DEFER row. Donor source asks for a synthetic-output/read-only structured result tool, which would open protocol/tool surface area and inherits the parked ADR's fake-evidence risk. OntoIndex found existing structured-content owners in `sanitize_mcp_tool_result_for_model`, `mcp_call_tool_result_output_schema`, app-server schema fixtures, and agent-job result paths, so there is no empty owner slot or single owner-local failing test gap. No promotion packet. |
| 2026-06-20 | 019 | NARROW | `ontocode-rs/protocol/src/request_user_input.rs`; `ontocode-rs/core/src/tools/handlers/request_user_input.rs`; tool deny path mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says extend current tool deny path, while donor source says ask-user tool with typed choices. Current code already has `RequestUserInputQuestionOption`, typed `options`, schema tests, feature-gate tests, integration round-trip/mode rejection tests, and MCP approval questions using typed accept/cancel/remember choices. OntoIndex reports `request_user_input.rs` is a 56-line protocol model file and `RequestUserInputHandler` has LOW impact. Deny/approval behavior is owned by MCP approvals, request-permissions, network denial, and guardian paths, not a single missing request-user-input gap. No promotion packet. |
| 2026-06-20 | 020 | NARROW | `ontocode-rs/state/src/runtime/agent_jobs.rs`; `ontocode-rs/core/src/tools/spec_plan.rs`; tool planner/task-output mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says test coverage around existing tool planner behavior, while donor source says explicit task-output tool separate from task control. OntoIndex reports `agent_jobs.rs` owns job create/get/list item/result/progress APIs and is 685 lines; `agent_jobs_tools_enabled` in `spec_plan.rs` has LOW impact. Current tests already cover normal vs worker exposure for `spawn_agents_on_csv`/`report_agent_job_result`, wrong-thread result rejection, and CSV export-on-completion. A paginated task-output read tool would require new state/API/tool surface, not a single existing-owner failing test gap. No promotion packet. |
| 2026-06-20 | 022 | NARROW | `ontocode-rs/core/src/session/turn_context.rs`; permission explanation mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says add only missing explanation fields and avoid a new permission engine, while donor source says preserve pre-plan permission mode and restore on exit. OntoIndex reports `turn_context.rs` is a 907-line owner with `permission_profile`, `build_per_turn_config`, and `make_turn_context`; current code carries `permission_profile` and `collaboration_mode` through turn construction and derives plan mode from `turn_context.collaboration_mode.mode`. Existing permission prompt/denial and plan/collaboration-mode restoration tests cover nearby behavior. No single owner-local failing test gap was found. No promotion packet. |
| 2026-06-20 | 027 | DEFER | `ontocode-rs/core/src/exec_policy.rs`; `ontocode-rs/core/src/config`; generated help/docs | parked | `gpt-5.4-mini` fallback | Stays parked. No fresh bug/regression/security/product evidence was found for the DEFER row. Donor source asks for wildcard permission rule docs in generated help, while the parked ADR says documentation/examples do not extend core. OntoIndex reports `exec_policy.rs` owns command approval policy, is 1050 lines, and `create_exec_approval_requirement_for_command` has CRITICAL upstream impact across 12 nodes / 6 modules. Existing wildcard/glob behavior has focused permission, config, network-proxy, sandboxing, and exec-policy tests. No promotion packet. |
| 2026-06-20 | 030 | DEFER | `ontocode-rs/core/src/session`; approval/plan-mode UX | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says multi-profile permission UX needs product demand first; donor source says batch approval plan mode for grouped changes in `core/src/session`. Duplicate gate found no reopen signal from Gemini or Oh My Pi reviews. OntoIndex reports `turn.rs` is the 2252-line turn/session owner with `run_turn`, `build_prompt`, and `built_tools`; worker also identified `core/src/session/handlers.rs:thread_settings_update` as live session-settings ownership reached from `update_thread_settings` and `user_input_or_turn_inner`. Existing permission-carry/plan-mode tests cover nearby behavior. No fresh bug/regression/security/product evidence was found. No promotion packet. |
| 2026-06-20 | 031 | NARROW | `ontocode-rs/core/src/tasks/review.rs`; review prompt/skill path | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says review command ideas belong in existing review prompt/skill path; donor source says add review command as first-class prompt command in `core/src/tasks/review.rs`. Current code already has `/review` as a TUI slash command with popup presets, inline custom args, app-server/session review target plumbing, and core review task ownership. OntoIndex reports `review.rs` has public `new` and `exit_review_mode`, is 279 lines, and worker found `core/src/session/handlers.rs:review` has LOW upstream impact through `submission_loop`, `resolve_review_request`, and `spawn_review_thread`. Existing prompt, TUI slash, and core review tests cover the behavior. No single owner-local failing test gap was found. No promotion packet. |
| 2026-06-20 | 032 | NARROW | `ontocode-rs/skills`; plugin-backed security review workflow | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says useful as workflow docs or skill, not runtime core; donor source says add security-review command as plugin-backed command in `core-skills` / `core-plugins`. Duplicate gate found Gemini removes Claude command/plugin overlaps and Oh My Pi explicitly blocks the related Claude security-review skill/hook surface. OntoIndex reports `ontocode-rs/skills/src/lib.rs` owns `system_cache_root_dir` and `install_system_skills`, is 170 lines, while worker found `SlashCommand::Review` maps to `open_review_popup()` in the existing TUI review path. Existing review, guardian, core-skills, and core-plugins tests cover adjacent loading/guardrail behavior. A dedicated `security-review` plugin command would be a new workflow surface, not one owner-local failing test gap. No promotion packet. |
| 2026-06-20 | 038 | NARROW | `ontocode-rs/core/src/tasks/review.rs`; structured review metadata | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says keep only as structured review metadata, not a new review service; donor source says add local-only review mode separate from remote review in `core/src/tasks/review.rs`. Duplicate gate found no Gemini dispatchable slice and no Oh My Pi reopen signal beyond narrow test-first hardening. OntoIndex reports `review.rs` owns public `new` and `exit_review_mode`, is 279 lines, and found `process_review_events`, `parse_review_output_event`, and `exit_review_mode` as structured review-output owners. Worker found `exit_review_mode` has one direct upstream caller, `ReviewTask.run`, emits `ExitedReviewModeEvent`, and has LOW impact. Existing review tests cover lifecycle/output, custom review model selection, session-model fallback, event filtering, and parent-history isolation. No local-only-vs-remote review split gap was proven without adding a second review service. No promotion packet. |
| 2026-06-20 | 039 | NARROW | `ontocode-rs/prompts/src/review_request.rs`; git/review prompt mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says git commit helpers should stay plugin/skill unless core state changes, while donor source says add branch diff preamble to review prompts in `core/src/tasks/review.rs`; these are different owner surfaces. Duplicate gate found Gemini has no dispatchable parked-row slice and Oh My Pi keeps Claude `NARROW`/`DEFER`/`REJECT` rows out of scope. OntoIndex reports `review_request.rs` exports `resolve_review_request`, `review_prompt`, `user_facing_hint`, and `REVIEW_PROMPT`, and is 138 lines. Current prompt code already has base-branch merge-base and `git diff` preambles with prompt tests; skill-loader/plugin tests cover nearby skill naming behavior. No single owner-local failing test gap was found. No promotion packet. |
| 2026-06-20 | 040 | DEFER | app-server / TUI review command; issue triage mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says issue triage workflow is useful but not core, while donor source says add PR selection fallback when PR number missing in app-server / TUI review command; these are different behaviors. Duplicate gate found Gemini keeps issue-triage automation external and Oh My Pi has no reopen lane for Claude command/UI/plugin overlaps. OntoIndex reports `app-server-protocol/src/protocol/v2/review.rs` is 66 lines and `tui/src/chatwidget/review_popups.rs` exposes review popup and branch/commit/custom prompt owners. Worker found `tui/src/branch_summary.rs` owns PR resolution with current-branch lookup and head-commit fallback, with `open_pull_request` LOW impact and two direct callers. Existing PR lookup and base-branch review tests cover nearby behavior. No fresh bug/regression/security/product evidence was found. No promotion packet. |
| 2026-06-20 | 041 | NARROW | GitHub skill/plugin; commit command mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says PR-body automation belongs in GitHub skill/plugin, while donor source says add commit command with scoped git allowlist in TUI command layer / core task; these are different behaviors. Duplicate gate found no matching Gemini or Oh My Pi pre-junior slice. OntoIndex reports `.codex/skills/codex-pr-body/SKILL.md` is the 62-line PR-body skill owner and search found existing TUI branch/PR metadata plus plugin/git owners. Worker found `tui/src/branch_summary.rs` owns TUI git/GitHub probes and `git-utils/src/baseline.rs` only has internal baseline commit scaffolding, not a user-facing commit command. Existing PR lookup tests cover current-branch view, parent-repo fallback, and open-PR parsing. A scoped commit command would add runtime commit automation, not one owner-local failing test gap. No promotion packet. |
| 2026-06-20 | 042 | NARROW | GitHub skill/plugin; commit-amend mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says CI diagnostics belong in GitHub skill/plugin first, while donor source says forbid commit amend unless explicitly asked in Git command prompt rules; these are different behaviors. Duplicate gate found no Gemini or Oh My Pi reopen slice. OntoIndex surfaced `permissions_instructions.rs` prompt assembly and `exec_policy.rs` amendment-rule tests as nearby git-policy owners; `.codex/skills/gh-fix-ci` is not indexed, but the GitHub plugin skill exists as the CI diagnostics owner. Worker found the live on-request permission template already warns about destructive actions like `rm` and `git reset`, but no dedicated `commit --amend` prompt test. This is not one stable existing-owner gap without moving commit workflow policy into runtime core. No promotion packet. |
| 2026-06-20 | 043 | NARROW | release notes; secret-warning mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says release note generation is non-core automation, while donor source says warn before committing likely secrets in Git command prompt rules; these are different behaviors. Duplicate gate found Gemini strips release/UI/eval/plugin overlap and Oh My Pi excludes release automation. OntoIndex search surfaced existing secret redaction owners in provider-auth, doctor output, external-agent import, and `secrets/src/sanitizer.rs`; worker found release-note ownership in `cli/src/doctor/output.rs` notes rendering plus TUI update-popup snapshots. The only nearby git rule is the generic no-commit instruction, not a dedicated secret-warning commit rule. No single owner-local failing test gap was found without creating commit-policy/runtime-core automation. No promotion packet. |
| 2026-06-20 | 044 | DEFER | repo workflow assistant; HEREDOC commit mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says repo workflow assistant should not enter core runtime, while donor source says use HEREDOC commit messages in Git command prompt rules; these are different behaviors. Duplicate gate found no exact Gemini or Oh My Pi duplicate and no reopen lane. OntoIndex reports `exec_policy.rs` owns `create_exec_approval_requirement_for_command`, is 1050 lines, and current HEREDOC-related tests include amendment suppression and prefix parsing cases. Worker found HEREDOC handling is already a shell/exec-policy concern and not a missing repo-workflow runtime feature. No fresh bug/regression/security/product evidence was found for this DEFER row. No promotion packet. |
| 2026-06-20 | 045 | NARROW | review templates; commit-style mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says review templates are useful as memory-bank/prompt assets, while donor source says include recent commit style in commit prompt rules; these are different behaviors. Duplicate gate found no exact Gemini or Oh My Pi duplicate. OntoIndex reports `git-utils/src/info.rs` exports `recent_commits` and other git-info helpers, is 890 lines, while search found review prompt tests and recent-commit/git-info tests as separate owners. Worker found `review_prompt` is owned by `prompts/src/review_request.rs`, `recent_commits` by `git-utils/src/info.rs`, and commit-request assembly by `exec/src/lib.rs`; these are split owners, not one shared commit-prompt seam. No single owner-local failing test gap was found without adding commit prompt/automation behavior into runtime core. No promotion packet. |
| 2026-06-20 | 046 | DEFER | changelog workflow; PR creation skill mismatch | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says changelog workflows are not core, while donor source says add PR creation command as plugin/skill in `core-skills` / GitHub plugin; both are non-core workflow/plugin territory and not the same behavior. Duplicate gate found no dispatchable Gemini or Oh My Pi slice. Local evidence shows GitHub PR creation already has a plugin skill owner in `github:yeet`, while PR-body editing lives in `codex-pr-body`. Worker found release-note/changelog-adjacent ownership in `cli/src/doctor/output.rs:notes_for_report` and release workflow scripts, with adjacent Changelog Writer and release-notes display tests. No fresh bug/regression/security/product evidence was found for this DEFER row. No promotion packet. |
| 2026-06-20 | 047 | DEFER | external issue triage workflow | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says marketplace-style triage belongs outside core; donor source says add duplicate issue triage workflow as external automation in `.github/workflows` / scripts. Duplicate gate found Gemini rejects issue-triage automation as product/runtime behavior and Oh My Pi has no reopen lane. OntoIndex reports `.codex/skills/codex-issue-digest/SKILL.md` is a 128-line external issue-digest owner and surfaced the issue-digest script family, including digest-row/ranking tests. Worker found live `.github/workflows/issue-deduplicator.yml` and `issue-labeler.yml` owners. No fresh bug/regression/security/product evidence was found for this DEFER row. No promotion packet. |
| 2026-06-20 | 048 | DEFER | external issue/reviewer workflow automation | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says reviewer assignment is workflow automation, not core; donor source says add issue lifecycle comment automation in `.github/workflows`. Duplicate gate found no Gemini dispatchable slice and only external-workflow-level overlap with Oh My Pi. Existing owners are `.github/workflows/issue-deduplicator.yml`, `.github/workflows/issue-labeler.yml`, and `.codex/skills/babysit-pr/scripts/gh_pr_watch.py`; worker found `fetch_new_review_items` already reads issue, review-comment, and review endpoints before CI checks. No fresh bug/regression/security/product evidence was found for this DEFER row. No promotion packet. |
| 2026-06-20 | 049 | DEFER | merge babysitting / workflow permissions automation | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says merge babysitting is plugin behavior; donor source says add a non-write-user CI check in `.github/workflows`. Duplicate gate found no Gemini or Oh My Pi reopen lane. Existing PR babysitting owner is `.codex/skills/babysit-pr`, whose watcher already checks CI, review state, and mergeability. Workflow permission evidence remains repository automation: explicit workflow permissions exist, and `.github/workflows/close-stale-contributor-prs.yml` already gates contributor PR automation on collaborator access before acting. No fresh bug/regression/security/product evidence was found for this DEFER row. No promotion packet. |
| 2026-06-20 | 050 | DEFER | branch hygiene / duplicate issue automation | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says branch hygiene automation is outside generated-code core; donor source says add auto-close duplicate issue flow with audit log in `.github/workflows` / scripts. Duplicate gate found no Gemini or Oh My Pi reopen lane. Existing owner is `.github/workflows/issue-deduplicator.yml`, which already fetches all/open issues, runs duplicate-identification prompts, normalizes outputs, comments with potential duplicates, and removes the trigger label. Issue summarization remains in `.codex/skills/codex-issue-digest`; branch hygiene remains separate repo automation in `.github/workflows/close-stale-contributor-prs.yml`. No fresh bug/regression/security/product evidence was found for this DEFER row. No promotion packet. |
| 2026-06-20 | 057 | NARROW | agent job list/status/kill interface | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says avoid duplicating current job list/status queries; donor source says separate task kill interface from spawn/render in `state`. Duplicate gate found no Gemini dispatchable slice and no Oh My Pi reopen lane. OntoIndex reports `agent_jobs.rs` is a 685-line owner with create/get/list/status/cancel/progress APIs; `mark_agent_job_cancelled` impact is LOW through `report_agent_job_result`, and `list_agent_job_items` impact is LOW through `run_agent_job_loop` and `spawn_agents_on_csv`. Existing core integration tests cover stop/cancel and status/progress shape. No exactly-one owner-local failing test gap was found; a separate kill interface would add a new surface. No promotion packet. |
| 2026-06-20 | 058 | NARROW | agent job origin / parent-child metadata | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says add only missing parent/child relationships if needed; donor source says add task type enum for local, remote, workflow, monitor in `protocol` / `state`. Duplicate gate blocks promotion because Oh My Pi explicitly blocks Claude agent/job/session overlap. Existing `SessionSource::SubAgent(ThreadSpawn)` carries parent thread id, depth, path, nickname, and role; `thread_spawn_edges` persists parent/child edges and has child/descendant tests; agent-job items already carry `source_id`. Worker found `persist_thread_spawn_edge_for_source` and turn metadata tests already cover parent lineage. No exactly-one owner-local failing test gap was found; a task type enum would add new protocol/state surface. No promotion packet. |
| 2026-06-20 | 059 | DEFER | agent job scheduler / background monitor task type | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says scheduler changes need concrete concurrency requirements; donor source says add background monitor task type in `state` / MCP monitor. Duplicate gate blocks promotion because Oh My Pi explicitly blocks Claude agent/job/session overlap for rows 057-059. Existing agent-job loop already clamps concurrency and handles `AgentLimitReached`; stop/cancel tests cover halting future items. MCP status/monitoring is already owned by `codex-mcp` connection manager and app-server MCP status processors; OntoIndex reports `connection_manager.rs` is an 823-line owner with readiness, startup failure, server-info, and resource/tool APIs. No fresh bug/regression/security/product evidence was found. No promotion packet. |
| 2026-06-20 | 066 | NARROW | autonomous memory extraction policy/caps | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says autonomous memory writes require explicit policy and caps; donor source says use forked subagent for memory extraction in `core/src/context_manager`. Duplicate gate blocks promotion because Gemini explicitly treats Claude memory/prompt-cache rows 066-078 as non-dispatchable duplicates. Existing owner is `ontocode-rs/memories/write`, not context-manager: startup gating skips ephemeral, disabled, and sub-agent sessions; rate limits, bounded claims, concurrency caps, retry backoff, redaction, and Phase 2 global locking already exist; Phase 2 already spawns a locked-down consolidation sub-agent. Existing tests cover startup, pruning, prompt shape, and rate limiting. No exactly-one owner-local failing test gap was found. No promotion packet. |
| 2026-06-20 | 067 | NARROW | memory-bank process / memory file permissions | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says current memory-bank process can absorb this as docs discipline, while donor source says strict file permissions for memory files in `thread-store` / memory layer. Duplicate gate blocks promotion because Gemini marks rows 066-078 as overlapping context/memory/prompt-cache scope. OntoIndex reports `memories/write/src/workspace.rs` is a 118-line owner with `prepare_memory_workspace`, `memory_workspace_diff`, `write_workspace_diff`, and baseline reset APIs; local evidence shows memory and thread-store writes use default file creation with no explicit permission-mode owner. No exactly-one owner-local failing test gap was found; strict memory-file permissions need explicit cross-platform policy and security evidence before promotion. No promotion packet. |
| 2026-06-20 | 068 | NARROW | memory read path / bounded context injection | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says keep as verification rule, not new storage; donor source says read current memory through the existing file-read path in `core/src/context`. Duplicate gate blocks promotion because Gemini keeps rows 066-078 in the context/memory/prompt-cache overlap bucket. Existing read owner is `ontocode-rs/memories/read`: `lib.rs` owns memory read-path helpers/citations and `usage.rs` classifies safe file reads/searches for `MEMORY.md`, `memory_summary.md`, `raw_memories.md`, rollout summaries, and memory skills. Existing context owners constrain hidden context through `InternalModelContextFragment` source validation and contextual-user-fragment tests, while `DiagnosticFragment` shows the hard-cap pattern. No exactly-one owner-local failing test gap was found without creating new storage or a parallel memory injection path. No promotion packet. |
| 2026-06-20 | 070 | DEFER | config / memory template personalization | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says personalization memory is risky without product boundary; donor source says allow a custom memory template path in config / memory layer. Duplicate gate keeps this in parked memory scope: Gemini defers memory/config boundary work and Oh My Pi keeps memory rows owner-local unless a real gap appears. OntoIndex reports `memories/write/src/prompts.rs` is a 132-line prompt owner exposing `build_consolidation_prompt` and `build_stage_one_input_message`; the consolidation prompt uses embedded templates plus memory extension blocks. Config already supports unrelated compact-prompt overrides, while phase-2 consolidation disables recursive memory use. No fresh bug/regression/security/safety/product evidence was found for a DEFER promotion. No promotion packet. |
| 2026-06-20 | 071 | DEFER | cross-project memory sharing / memory update prompt | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says cross-project memory sharing should not be core now; donor source says allow a custom memory update prompt in config / memory layer, so the ADR row and donor source do not describe one exact feature. Duplicate gate keeps this in parked memory/context/prompt-cache scope. Existing ownership is split: `memories/read` owns read injection/citations/usage classification, `memories/write` owns Phase 1/2 prompt rendering and embedded templates, config exposes compact/guardian prompt overrides but no memory-update prompt key, and TUI memory settings only persist `use_memories` / `generate_memories` while `/memory update` remains unavailable. No fresh bug/regression/security/safety/product evidence was found for a DEFER promotion. No promotion packet. |
| 2026-06-20 | 073 | NARROW | context-fragment memory token cap | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says respect existing context-fragment architecture; donor source says add a total session memory token limit in `core/src/context`. Duplicate gate keeps this in the Gemini-overlapping context/memory/prompt-cache bucket. Existing cap behavior is already split across owners: `AdditionalContext*Fragment` caps individual additional-context values, `DiagnosticFragment` caps diagnostics, realtime startup context has per-section and total budgets, AGENTS.md loading uses byte caps, memory stage-one prompt input truncates to 70% of the effective context window, and compaction clamps token limits. OntoIndex marks `Session.build_initial_context` as CRITICAL impact, with 44 impacted nodes and `run_turn` affected. No exactly-one owner-local failing test gap was found without adding a parallel injection path or changing high-blast-radius context assembly. No promotion packet. |
| 2026-06-20 | 075 | NARROW | memory-bank practice / key-results preservation | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says memory docs can be improved outside core; donor source says keep exact user-requested outputs in memory key-results as `.memory-bank` practice. Duplicate gate keeps this in the Gemini-overlapping context/memory/prompt-cache bucket and Oh My Pi does not reopen it. Current `.memory-bank/MEMORY.md` is a linked memory layer, `project_plan-current.md` says the initial core-natural slice is complete, and memory write templates already define `rollout_summary`, `raw_memory`, and `MEMORY.md` consolidation guidance. This is docs/process hygiene, not a core implementation gap. No promotion packet. |
| 2026-06-20 | 076 | NARROW | memory-bank practice / failed approaches | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says command guidance belongs in prompt/docs; donor source says track failed approaches in memory as `.memory-bank` practice. Duplicate gate keeps this in the Gemini-overlapping context/memory/prompt-cache bucket. Current memory templates already instruct Phase 1 and Phase 2 to preserve failure shields, what failed, what worked instead, recurring failure modes, and how future agents should do it differently. Lean-ctx memory-bank tooling remains repo-only scripts/doc checks. This is docs/process hygiene, not a core implementation gap. No promotion packet. |
| 2026-06-20 | 077 | NARROW | memory-bank tracking / current-state section | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says project memory status can stay memory-bank tracking; donor source says add current-state memory section as a mandatory update target in `.memory-bank`. Duplicate gate keeps this in the Gemini-overlapping context/memory/prompt-cache bucket, and Oh My Pi does not reopen it. Existing `.memory-bank/project_plan-current.md` already has `Current Status`, counts, and next-phase tracking; `.memory-bank/project_pending-tasks.md` carries active task status/outcomes/next actions; `scripts/onto_memory_tools.py` provides read-only `status_digest`. This is docs/process hygiene, not a core implementation gap. No promotion packet. |
| 2026-06-20 | 078 | NARROW | memory-bank workflow commands / ADR hygiene | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says ADR hygiene belongs in docs process; donor source says add workflow commands section in memory as `.memory-bank` practice. Duplicate gates keep Claude context/memory/prompt-cache/command overlap out of implementation dispatch. Existing memory-bank index links active plan/backlog, prior row 077 records current-state tracking in `project_plan-current.md` and `project_pending-tasks.md`, memory templates already preserve workflow/tool sequences, verification checklists, exact commands, and failure guidance, and `scripts/onto_memory_tools.py` provides repo-only read helpers. This is docs/process hygiene, not a core implementation gap. No promotion packet. |
| 2026-06-20 | 081 | DEFER | read-only prompt suggestion/speculation overlay | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says speculative generation is risky and cache-hostile by default; donor source says add prompt suggestion/speculation in read-only overlay under `core/src/context_manager` / TUI with overlay cleanup test. Duplicate gates keep rows 081-095 in the parked context/memory/prompt-cache/speculation bucket. OntoIndex did not find a clean existing owner for speculative prompt suggestions; local evidence shows `next_prompt_suggestion` only in markdown-render fixture text, not current core files. Existing prompt/cache surfaces are broad session/model-client and TUI overlay owners, so this would require new product/runtime behavior rather than one existing-owner failing test. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 082 | DEFER | background speculation max-turn bound | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says background speculation needs measured latency win first; donor source says limit speculation to max turns under `core/src/context_manager` with a max-turn test. Duplicate gates keep rows 081-095 in the parked context/memory/prompt-cache/speculation bucket. Existing `core/src/context_manager` only exposes history, normalization, and update owners; no background speculation implementation or max-turn owner exists to harden. OntoIndex results pointed to compaction and generic tool-suggestion surfaces, not a prompt-speculation runtime. No measured latency/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 083 | DEFER | prediction cache / speculation max-message bound | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says prediction cache should not be introduced without telemetry; donor source says limit speculation to max messages under `core/src/context_manager` with a message-cap test. Duplicate gates keep rows 081-095 in the parked context/memory/prompt-cache/speculation bucket. Existing `ContextManager` owns history, normalization, update plumbing, token accounting, and an image-byte estimate cache, not a prediction/speculation cache. OntoIndex surfaced prompt-cache-key and generic API telemetry, but no row-specific prediction-cache telemetry or max-message owner. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 084 | NARROW | speculation safe read-only tool list / prompt-cache constraint | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says keep only deterministic prompt caching constraints; donor source says only allow safe read-only tools in speculation under `core/src/tools/spec_plan.rs` with a tool-list test. `spec_plan.rs` is a real generic tool-router owner, but current tests already cover deterministic visible-tool ordering and exposure gating. No speculation-specific owner-local failing test exists without introducing speculative runtime/tool behavior, which the parked ADR explicitly blocks. No promotion packet. |
| 2026-06-20 | 085 | DEFER | speculative overlay write adoption / multi-branch generation | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says multi-branch generation is expensive and hard to verify; donor source says copy accepted speculative writes from overlay to main workspace only under `exec-server` with an overlay-copy test. Duplicate gates keep rows 081-095 in the parked context/memory/prompt-cache/speculation bucket. OntoIndex found no overlay/copy-back symbols. Existing exec-server copy and env-overlay plumbing is generic filesystem/process infrastructure, not speculative write adoption. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 086 | DEFER | speculation overlay cleanup / autocomplete product scope | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says autocomplete speculation is UI/product work; donor source says remove speculation overlay with retry under `exec-server` with a cleanup test. Existing exec-server cleanup/retry ownership is generic process-retention cleanup and tempdir-based test fixtures, not a speculation-overlay cleanup/retry owner. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion, and no exactly-one owner-local failing test gap was found. No promotion packet. |
| 2026-06-20 | 089 | NARROW | speculation boundary telemetry / lint-only scope | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says use as lint/check, not runtime rewrite; donor source says track speculation boundary type/tool/detail under `otel` with a telemetry redaction test. Existing OTEL coverage already has generic export-routing and redaction tests for prompt/user metadata and auth/request metadata, but no speculation-boundary runtime or lint owner exists to harden. No exactly-one owner-local failing test gap was found without introducing speculation runtime. No promotion packet. |
| 2026-06-20 | 090 | NARROW | shell directory-change suppression / bounded compression rule | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says keep as bounded compression rule; donor source says suppress speculation when command changes directories under `execpolicy` / shell parser with a command parser test. Existing `shell-command` parser coverage already handles `cd` as command path context, command safety treats `cd` as safe, and execpolicy already has shell-wrapper/path-context behavior. No exactly-one owner-local failing parser or execpolicy test gap was found without introducing speculation runtime. No promotion packet. |
| 2026-06-20 | 094 | NARROW | subagent token budget diagnostics / no context mutation | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says use only for diagnostics, not context mutation; donor source says keep token budget disabled for subagents by default under `core/src/agent` with a subagent budget test. Existing multi-agent control/config owners cover spawn/resume/send/list/status and subagent usage-hint defaults, but there is no token-budget field, diagnostics default, or owner-local test gap to harden. No exactly-one owner-local failing test gap was found without adding continuation runtime or context mutation. No promotion packet. |
| 2026-06-20 | 095 | NARROW | budget continuation prompt nudge / prompt stability | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says avoid frequent prompt mutation and enforce stability; donor source says add a model-visible nudge message for budget continuation under prompts/context fragments with a prompt fixture. Existing goal prompt fixtures already cover continuation and budget-limit prompt behavior, the runtime path injects the existing hidden `goal` context item, and session tests cover budget-limited steering without aborting the turn. No exactly-one owner-local failing fixture gap was found without adding prompt churn or a new context fragment. No promotion packet. |
| 2026-06-20 | 097 | NARROW | agent job state classification / bounded redacted hook output | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says hook output must be bounded and redacted, while donor source says classify dispatched job state after each turn under `state` / agent jobs with a job classifier fixture. These are mismatched surfaces: agent jobs already have lifecycle/result tests and status/progress classification, while hook output already has bounded spill coverage. No exactly-one owner-local failing bounded/redacted classifier fixture gap was found without adding a new job lifecycle API, protocol event, or hook owner. No promotion packet. |
| 2026-06-20 | 101 | NARROW | teammate idle hook / existing hook registry only | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says avoid a second hook registry; donor source says add teammate idle hook under `hooks` / agent jobs with a hook event test. Existing hook registry/protocol event surface has no teammate-idle hook event to test, and the only idle signal found is an extension lifecycle path rather than a hooks registry event. No exactly-one owner-local matcher/event test gap was found without adding a new hook/protocol/config surface or second registry. No promotion packet. |
| 2026-06-20 | 102 | NARROW | task-completed hook / matcher-tests-only boundary | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says extend matcher tests only; donor source says add task completed hook under `hooks` with a hook payload test. The current hook surface has no task-completed event, matcher behavior is already covered for supported and unsupported events, and stop-hook terminal payload/continuation behavior is already tested. No exactly-one owner-local matcher/payload fixture gap was found without adding a new hook event or job-lifecycle surface. No promotion packet. |
| 2026-06-20 | 103 | NARROW | stop-hook summary message / bounded declarative policy | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says keep hook policy declarative and bounded; donor source says add stop hook summary message under `core/src/session` with a context fixture. Existing bounded stop-hook feedback behavior lives in the hooks owner, and the hook suite already covers continuation-prompt persistence, resumed history, multiple blocking stop hooks, and spill behavior. No exactly-one owner-local session context fixture gap was found without adding new hook policy semantics or prompt/context churn. No promotion packet. |
| 2026-06-20 | 104 | NARROW | structured hook progress events / existing hooks tests-docs only | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says existing hooks should absorb this as tests/docs; donor source says store hook progress as structured events under `protocol` / `hooks` with a protocol fixture. Existing hooks already have structured run summaries, completed events, output entries, event-specific wire shapes, and hook-family fixture coverage. No exactly-one owner-local protocol/hooks fixture or docs gap was found without adding new progress-event protocol semantics or app-server/TUI rendering scope. No promotion packet. |
| 2026-06-20 | 106 | DEFER | bridge feature gate / platform work | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says remote bridge is platform work, not immediate core; donor source says add bridge feature gate with import-safe stubs under `app-server` / bridge owners with a feature-off compile test. Local bridge references are unrelated accepted owners such as zsh exec bridge, UDS/transport bridge, network proxy bridge, and generic TUI stubs; no IDE/remote bridge owner or feature-off gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 107 | DEFER | no-op bridge handle / browser bridge demand gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says browser/web bridge should wait for app-server demand; donor source says add no-op bridge handle under `app-server` / TUI with a disabled bridge test. Generic TUI unsupported-feature stubs and browser/login/open-url references exist, but no disabled bridge owner, no bridge-handle surface, and no fresh app-server demand evidence were found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 108 | DEFER | bridge state enum / bridge auth ADR gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says bridge auth needs ADR and compatibility tests first; donor source says add a bridge state enum under `app-server-protocol` with a serialization test. Existing `remoteControl/status/read` and `RemoteControlConnectionStatus` already expose disabled/connecting/connected/errored state through protocol responses, notifications, README text, generated JSON schema, and generated TypeScript. No fresh bug/regression/security/safety/product evidence or missing compatibility-test owner was found for adding another bridge auth/state API. No promotion packet. |
| 2026-06-20 | 109 | DEFER | bridge session spawn modes / transport duplication gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says bridge transport should not duplicate MCP/app-server; donor source says add bridge session spawn modes under `app-server` / exec-server with a config enum test. Existing owners already cover remote-control enable/status, exec-server local/remote environment selection, and thread-start cwd/workspace-root overrides, but no bridge session-spawn mode config owner or failing config enum test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 110 | DEFER | proactive bridge token refresh / app-server protocol proof gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says bridge protocol must prove app-server cannot serve it; donor source says use JWT expiry to schedule proactive bridge token refresh under auth / app-server with a token refresh scheduler test. Existing owners already refresh remote-control server tokens before expiry, recover after websocket unauthorized responses, support ChatGPT proactive refresh by JWT expiration, and refresh RMCP OAuth tokens with skew. No bridge-specific protocol proof, fresh disconnect evidence, or missing scheduler-test owner was found. No promotion packet. |
| 2026-06-20 | 111 | DEFER | trusted-device token boundary / external-agent ADR overlap gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says external-agent interop already has ADR context and to avoid overlap; donor source says add trusted-device token boundary under login/auth with a redacted token test. Existing auth owners already include agent-identity auth, external-auth/provider credential redaction coverage, Gemini OAuth debug redaction, and sensitive URL redaction. The external-agent interop ADR keeps Stage 0 read-only/redacted and blocks credential persistence or deeper auth import without a separate ADR. No fresh bug/regression/security/safety/product evidence or missing redaction-test owner was found. No promotion packet. |
| 2026-06-20 | 112 | DEFER | workspace-scoped work secret / bridge resource sync gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says bridge resource sync risks parallel state; donor source says add workspace-scoped work secret type under app-server / auth with a decode/validate test. Existing owners cover app-server websocket bearer/shared-secret validation, MCP bearer env-var config, auth env handling, and operational-evidence secret rejection, but no workspace-scoped work secret owner or failing decode/validate test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 113 | DEFER | bridge message UUID dedupe / remote execution security gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says remote execution needs security review first; donor source says deduplicate inbound/outbound bridge message UUIDs under app-server with a bounded set test. Existing owners cover remote-control segmented chunk duplicate handling, command/exec duplicate process IDs, exec-server body stream IDs, and relay test duplicate stream responses, but no bridge whole-message UUID dedupe owner or failing bounded-set test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 114 | DEFER | bridge outbound write batching / app-server event-stream gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says bridge event streaming belongs in app-server if needed; donor source says batch bridge outbound writes under app-server with a flush gate test. Existing remote-control transport already has bounded outbound buffering, acks, segmented ack advancement, queue capacity, and backpressure behavior, but no bridge-specific outbound batching owner or failing flush-gate test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 115 | DEFER | bridge permission delegation path / app-server v2 reuse gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says reuse app-server v2 protocol, not donor-specific bridge; donor source says add bridge permission delegation path under app-server / core approvals with a permission response fixture. Existing app-server v2 already has `item/permissions/requestApproval`, `PermissionsRequestApprovalResponse`, client-response handling, round-trip tests, and core session request-permissions event tests. No fresh bug/regression/security/safety/product evidence or concrete missing existing-owner fixture gap was found. No promotion packet. |
| 2026-06-20 | 116 | DEFER | bridge attachments ingestion / concrete protocol gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says cross-process bridge tests need concrete protocol; donor source says add bridge attachments ingestion under app-server / protocol with an attachment parsing test. Existing typed `turn/start` input supports text, remote images, local images, skills, and mentions, and feedback upload has log attachment handling, but no bridge-specific attachment protocol owner or failing attachment parsing test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 117 | DEFER | bridge status TUI indicator / browser-extension non-core gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says browser extension ideas are not core; donor source says add bridge status TUI indicator with a snapshot test. Existing app-server remote-control status APIs/tests and TUI `/status` remote connection display already cover current connection-health surfaces, but no bridge/browser-extension status contract or failing TUI snapshot gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 118 | DEFER | bridge diagnostics command / IDE-extension non-core gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says IDE extension ideas are not core runtime; donor source says add bridge diagnostics command under TUI / app-server with a command output fixture. Existing CLI/app-server remote-control start/status output, daemon output serialization, websocket HTTP error details, and `/readyz`/`/healthz` tests already cover current diagnostics-adjacent surfaces, but no bridge-specific diagnostics command owner or failing command-output fixture gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 119 | DEFER | bridge env-less transport abstraction / remote workspace mapping risk gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says remote workspace mapping is high-risk; donor source says add bridge env-less transport abstraction under app-server with a transport trait test. Existing app-server transports, remote-control websocket runtime, remote-control transport tests, and remote exec environment transport selection already cover current transport owners, but no bridge-specific env-less transport owner or failing trait test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 120 | DEFER | bridge teardown flush / platform decision gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says bridge status UI is downstream of platform decision; donor source says add bridge teardown flush before close under app-server with a teardown test. Existing stdio/remote-control writers signal write completion, remote-control outbound buffers are acked, close-client forwarding waits for queue capacity, and websocket shutdown is covered, but no bridge-specific teardown flush owner or failing lost-final-message test gap was found. No fresh bug/regression/security/safety/product evidence was found for DEFER promotion. No promotion packet. |
| 2026-06-20 | 122 | NARROW | MCP list-tools source metadata / existing resources only | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says add metadata to existing MCP resources only if missing; donor source says add list-tools MCP endpoint with source metadata under `codex-mcp` with a list tools test. Existing RMCP `tools/list`, `McpConnectionManager::list_all_tools`, server metadata attachment, MCP resource list tools, and MCP approval metadata already cover current tool/resource metadata paths. Adding source metadata would create a new source-browsing/API surface rather than exactly one existing-owner fixture gap. No promotion packet. |
| 2026-06-20 | 123 | DEFER | list-commands MCP endpoint / source-browsing policy gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says MCP source browsing must not bypass OntoIndex/security policy; donor source says add list-commands MCP endpoint under app-server / commands with a command registry test. Existing TUI slash-command registry/filtering and tests already cover command discovery inside the UI, and MCP tool listing already has an owner, but no fresh evidence or concrete existing app-server command-registry gap was found. Adding command listing over MCP would create new command/source-browsing API surface. No promotion packet. |
| 2026-06-20 | 128 | DEFER | architecture overview MCP prompt / diagnostic explorer UI gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says diagnostic explorer UI can wait; donor source says add architecture overview MCP prompt under docs/MCP with a prompt fixture. Existing architecture overview lives in memory-bank/OntoIndex docs, and no concrete docs/MCP prompt owner or failing prompt-fixture test gap was found. Adding an MCP prompt would create a new docs/MCP API/prompt surface and diagnostic explorer UI path without fresh bug/regression/security/safety/product evidence. No promotion packet. |
| 2026-06-20 | 129 | DEFER | compare-tools MCP prompt / teaching-demo server gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says MCP teaching/demo server is documentation/plugin work; donor source says add a compare-tools MCP prompt under docs/MCP with a prompt fixture. No existing compare-tools prompt owner, failing prompt fixture, or fresh bug/regression/security/safety/product evidence was found. Adding the prompt would create a new docs/MCP prompt or teaching-server surface instead of extending a current core owner. No promotion packet. |
| 2026-06-20 | 130 | NARROW | MCP custom source-root env / resource caps-redaction current-manager gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row allows MCP resource caps/redaction only if added to the current manager, but donor source asks for custom source-root env under `codex-mcp`. Existing resource list/read paths use `McpConnectionManager`, core MCP resource handlers, and shared function-output truncation, while app-server resume redaction already covers MCP tool-call results. No exactly-one current-manager failing cap/redaction fixture was found, and custom source-root env would add disallowed source-browsing/config surface. No promotion packet. |
| 2026-06-20 | 131 | NARROW | slash command type metadata / existing command-system gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says slash command metadata should extend the existing command system; donor source asks for command types prompt/local/local-UI with a command registry enum test. Existing `SlashCommand` metadata already owns descriptions, command names, inline-arg support, side-conversation availability, task availability, and visibility, with popup/filtering/dispatch consumers. No exactly-one failing command-type metadata test gap was found; prompt/local/local-UI would add a new classification dimension. No promotion packet. |
| 2026-06-20 | 133 | NARROW | command progress message field / command discovery duplication gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says command discovery must not duplicate tools; donor source asks for a command progress message field in the TUI command layer with a snapshot test. Existing command metadata owns names/descriptions/availability, slash dispatch owns per-command loading and in-progress rejection messages, and specific commands already add targeted loading UI where needed. No exactly-one failing progress-message metadata or snapshot gap was found; a generic field would add new command-progress surface. No promotion packet. |
| 2026-06-20 | 135 | NARROW | moved-to-plugin command wrapper / generated command-docs metadata gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says command docs can be generated from metadata; donor source asks for a moved-to-plugin command wrapper in `core-plugins` with a legacy command test. Existing `SlashCommand` metadata and command popup rendering already generate user-visible command rows from command names/descriptions/availability, and no exactly-one generated-docs metadata gap was found. Moving commands to plugin wrappers would add a new command/plugin runtime surface outside the narrow path. No promotion packet. |
| 2026-06-20 | 136 | NARROW | markdown command frontmatter parser / docs-skill asset gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says user command examples are docs/skill assets; donor source asks for a frontmatter parser for markdown commands under `core-skills` / plugins with frontmatter tests. Existing skill frontmatter parsing lives in `core-skills` with valid/invalid/metadata tests, and plugin manifest parsing lives in `core-plugins`. No exactly-one failing owner-local frontmatter gap was found; markdown-command parsing would add a new command/plugin runtime surface instead of extending current skill asset parsing. No promotion packet. |
| 2026-06-20 | 137 | DEFER | shell substitutions in prompt commands / speculative marketplace gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says custom command marketplace is speculative; donor source asks to execute shell substitutions in prompt commands with an allowlist under the command layer. Existing slash commands are static TUI dispatch metadata, shell execution is already routed through shell-command handlers and exec policy, and shell parsing tests intentionally reject variable/command substitutions in restricted parsing paths. No fresh bug/regression/security/safety/product evidence or concrete owner-local allowlist gap was found; prompt-command substitutions would add a new command runtime surface. No promotion packet. |
| 2026-06-20 | 138 | NARROW | `/doctor` diagnostics screen / GitHub-plugin workflow gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says commit command belongs in GitHub/plugin workflow; donor source asks for `/doctor` style diagnostics as a structured TUI/app-server screen with snapshot and API tests. Existing diagnostics/status owners already include CLI doctor output, TUI `/status` and `/debug-config`, app-server feedback doctor report, health/readiness, remote-control status, MCP status, and config warnings. No exactly-one owner-local snapshot/API gap was found; a structured `/doctor` screen would add a new user-facing diagnostics surface. No promotion packet. |
| 2026-06-20 | 139 | NARROW | `/status` session/system summary / review-skill ownership gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says review command belongs in review skill/prompt; donor source asks to add `/status` session/system summary in TUI with a snapshot test. Existing `/status` already owns compact operational state through TUI status card/format modules, dispatch, rate-limit refresh, instruction-source rendering, token usage, forked-from display, permission/profile fields, and snapshot coverage. No exactly-one owner-local session/system summary gap was found; adding another interpretation would create status-surface churn outside the narrow gate. No promotion packet. |
| 2026-06-20 | 140 | NARROW | `/cost` usage summary command / review-skill-hook gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says security-review command belongs in review skill/hook surface; donor source asks to add `/cost` or usage summary command under token usage / TUI with a usage fixture. Existing `/status` already owns spend visibility through token usage, context-window, rate-limit, credits, and limit snapshots; app-server already exposes token usage replay and account rate-limit/credits plumbing. No exactly-one owner-local usage fixture gap was found; `/cost` would add a new slash command/public status surface. No promotion packet. |
| 2026-06-20 | 141 | NARROW | `/files` context listing / existing approval-path gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says command execution policy should reuse existing approval path; donor source asks to add `/files` context listing under context manager / TUI with a context snapshot. Existing owners already cover file/context affordances through `@` file mentions, `/ide` active-file/open-tabs prompt context, `/status` instruction/context-window output, `/debug-config` config/requirement inspection, and centralized tool approval orchestration. No exactly-one context snapshot/doc gap was found; `/files` would add a new slash command and context-inspection surface. No promotion packet. |
| 2026-06-20 | 143 | NARROW | `/add-dir` scoped workspace roots / compact telemetry gate | parked | `gpt-5.4-mini` fallback | Stays parked. Parked row says command telemetry should be compact and opt-in; donor source asks to add `/add-dir` with scoped workspace roots under config / permissions with a permission root test. Existing `/sandbox-add-read-dir`, additional-dir warnings, permission profile workspace roots, runtime workspace roots, app-server command cwd scoping, turn-start root rebinding, and Windows sandbox root tests already cover this space. No exactly-one owner-local permission-root gap was found; `/add-dir` would add a duplicate slash-command surface. No promotion packet. |
| 2026-06-20 | 144 | DEFER | `/output-style` prompt fragments / release-automation gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/output-style` via prompt fragments under prompts / config with a fragment test; parked ADR row says release command is automation, not core. Existing owners already cover style control through `/personality`, `Feature::Personality`, `PersonalitySpecInstructions`, session personality updates, prompt fragments, and personality integration/snapshot tests. No fresh bug/regression/security/safety/product evidence or concrete owner-local prompt/config fragment test gap was found; `/output-style` would add a duplicate slash-command/style runtime surface. No promotion packet. |
| 2026-06-20 | 145 | NARROW | `/fast` config overlay / eval-command tooling gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/fast` mode as a config overlay under model-provider / config with a config overlay test; parked ADR row says eval command can live in tooling first. Existing owners already cover fast/low-latency behavior through `service_tier`, Fast Mode feature gating, service-tier config writes, catalog-driven `/fast` slash command discovery/dispatch, keybinding toggle, app-server session forwarding, and config/status/slash-command tests. No exactly-one model-provider/config test or doc gap was found; adding another `/fast` mode would duplicate current service-tier surface. No promotion packet. |
| 2026-06-20 | 146 | NARROW | `/effort` reasoning command / dev-only tool-call debugging gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/effort` mapped to reasoning effort under config / model-provider with a turn config test; parked ADR row says tool-call command debugging should be dev-only. Existing owners already cover reasoning selection through `/model`, reasoning popup selection/persistence, reasoning shortcuts, `model_reasoning_effort` config edits, app-server turn/thread `effort` parameters, resume fallback docs, and TUI/app-server/config tests. No exactly-one config/model-provider turn-config test or doc gap was found; `/effort` would add duplicate command surface. No promotion packet. |
| 2026-06-20 | 147 | NARROW | `/model` picker command / MCP manager diagnostics gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/model` picker command under models-manager / TUI with a picker snapshot; parked ADR row says MCP command debugging extends existing manager diagnostics. Existing owners already cover `/model` command metadata, slash dispatch to `open_model_popup`, grouped provider model picker, all-model fallback, provider catalog grouping, hidden/unsupported model filtering, service-tier command insertion after `/model`, and picker/reasoning snapshots. No exactly-one models-manager/TUI picker snapshot gap was found. No promotion packet. |
| 2026-06-20 | 148 | NARROW | `/permissions` command / existing config-owner gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/permissions` command under config / TUI so users can inspect approval rules, with a config edit test; parked ADR row says config command should not add new config owner. Existing owners already cover the `/permissions` command, permission-profile picker, permission/profile config persistence, `/status` permission summaries, `/debug-config` requirement inspection, app-server permission protocol types, and focused TUI/config tests. No exactly-one config/TUI permission inspection or config-edit gap was found. No promotion packet. |
| 2026-06-20 | 149 | NARROW | `/hooks` command / current-session-state gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/hooks` command under hooks / TUI to make hook state manageable, with a hook list test; parked ADR row says session command should reuse current session state. Existing owners already cover `/hooks` command metadata/dispatch, TUI `hooks/list` fetch and normalization, app-server `hooks/list` RPC, hooks browser, hook trust updates, hook live/history cells, debug-config managed-hook visibility, and app-server/TUI hook-list tests. No exactly-one hooks/TUI hook-state listing gap was found. No promotion packet. |
| 2026-06-20 | 150 | NARROW | `/skills` command / rollout-session-flow gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add `/skills` command under core-skills / TUI to make skill availability visible, with a skill list test; parked ADR row says resume command belongs in current rollout/session flow. Existing owners already cover `/skills` command metadata, TUI menu/list/manage flows, app-server `skills/list`, `skills/changed`, core-skills visible rendering, skill config writes, and user-facing `/skills` discovery. No exactly-one core-skills/TUI skill-availability test or doc gap was found. No promotion packet. |
| 2026-06-20 | 151 | NARROW | plugin marketplace command / skill-manager-UI gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add a plugin marketplace command under core-plugins / app-server to manage plugins without manual files, with a plugin command test; parked ADR row says skill command polish belongs in skill manager/UI. Existing owners already cover `/plugins`, plugin list/detail UI, marketplace add/remove/upgrade, plugin install/uninstall, app-server plugin/marketplace RPCs, core plugin install storage, and TUI/app-server tests/snapshots. No exactly-one plugin manager/UI test or doc gap was found. No promotion packet. |
| 2026-06-20 | 152 | NARROW | reload-plugins command / plugin-manager-UI gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add a reload-plugins command under core-plugins for a faster plugin dev loop, with a cache clear test; parked ADR row says plugin command polish belongs in plugin manager/UI. Existing owners already cover plugin/marketplace RPCs, TUI plugin-list refresh, plugin cache cleanup, marketplace upgrade refresh, curated/non-curated cache refresh, remote installed cache refresh, and TUI refresh preservation tests. No exactly-one plugin manager/UI reload or cache-refresh test/doc gap was found. No promotion packet. |
| 2026-06-20 | 153 | DEFER | terminal setup command / speculative command-packs gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add a terminal setup command under TUI / install context to improve shell integration, with a platform-gated snapshot; parked ADR row says command packs are speculative. Existing terminal work is split across diagnostics, startup probing, title setup, keyboard handling, runtime TUI initialization, and Windows sandbox setup, but no fresh bug/regression/security/safety/product evidence or concrete install/TUI test gap was found. A setup command would add new installer/shell-integration/product surface. No promotion packet. |
| 2026-06-20 | 154 | DEFER | remote-env command / localization-non-core gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add a remote-env command under app-server / config to make remote execution setup explicit, with a config fixture; parked ADR row says command localization is not core now. Existing owners already cover `environment/add`, thread/turn environment selection, remote-control environment IDs, command/process env overrides, exec-server environment management, remote-env routing, and related app-server/core tests. No fresh bug/regression/security/safety/product evidence or concrete app-server/config test gap was found. No promotion packet. |
| 2026-06-20 | 155 | NARROW | sandbox-toggle command / approval-metadata gate | parked | `gpt-5.4-mini` fallback | Stays parked. Donor source asks to add a sandbox-toggle command under permissions / TUI for controlled permission mode changes, with a permission profile test; parked ADR row says command permissions can extend existing approval metadata. Existing owners already cover `/permissions`, `open_permission_profiles_popup`, permission profile selection, approval policy updates, permission history messages, status permission summaries, app-server permission protocol, and TUI/app-server tests. No exactly-one permissions/TUI permission-profile test or doc gap was found. No promotion packet. |
| 2026-06-20 | 156 | NARROW | session rename/tag commands / generated-command-docs gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing TUI `/rename` command and chatwidget slash-command tests already cover queued/inline rename flows, while thread-store metadata tests cover persisted title/metadata propagation. Adding tags would introduce new command/session-organization surface, and no exactly-one existing-owner thread-store/TUI metadata doc or test gap was found. No promotion packet. |
| 2026-06-20 | 157 | NARROW | export session command / shell-sandbox-owner gate | parked | `gpt-5.4-mini` fallback | Stays parked. Current session command/API surface covers archive/unarchive and rollout-trace diagnostics, but no existing session export command, export API, fixture path, or shell/sandbox owner-local gap exists to harden. Adding export would create new user-facing artifact/export surface and possibly new format/API behavior. No promotion packet. |
| 2026-06-20 | 158 | DEFER | share command consent gate / workflow-macros gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing app-server transport auth, remote-control pairing/auth recovery, app-server permission approval, feedback-upload consent, and plugin-share tests already cover adjacent consent/security surfaces. No fresh bug/regression/security/safety/product evidence or concrete app-server consent gap was found for a share command, and adding one would create new collaboration/API/upload surface. No promotion packet. |
| 2026-06-20 | 159 | NARROW | history search dialog / memory-bank-skills gate | parked | `gpt-5.4-mini` fallback | Stays parked. Resume picker is already searchable, has search/error/search-line snapshot coverage, and thread-store already exposes search-term/list behavior with TUI lookup using backend title search. No exactly-one existing resume-picker/thread-store search gap was found; adding a dialog would create new session-management UI surface. No promotion packet. |
| 2026-06-20 | 160 | NARROW | global command/file search dialog / command-fixtures gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing command popup and file search popup are separate covered TUI owners, with command popup snapshots, file search orchestration, generic searchable selection views, and keymap picker search coverage. No exactly-one command/file picker fixture gap was found; a combined global dialog would add new navigation surface. No promotion packet. |
| 2026-06-20 | 161 | NARROW | typed plugin install errors / TUI-polish gate | parked | `gpt-5.4-mini` fallback | Stays parked. Core-plugins already has typed install/remote sync errors, and TUI plugin install/detail failures already route through existing plugin detail error popup states with snapshot coverage; install success/refresh behavior is also tested. No exactly-one plugin install/error snapshot or typed-state gap was found. No promotion packet. |
| 2026-06-20 | 162 | NARROW | MCP server approval dialog copy tests / review-pane-snapshot gate | parked | `gpt-5.4-mini` fallback | Stays parked. Core MCP tool-call tests already assert server-named and app-labeled approval copy variants, and TUI MCP server elicitation snapshots cover approval fallback, session-persist, and param-summary variants. No exactly-one MCP trust/approval dialog copy snapshot gap was found. No promotion packet. |
| 2026-06-20 | 163 | DEFER | MCP import multiselect dialog / UI-product-demand gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing external-agent MCP import service/tests cover MCP import without a TUI multiselect, and reusable TUI multiselect widgets already have snapshot coverage in other setup flows. No fresh bug/regression/security/safety/product evidence or concrete MCP-import multiselect snapshot gap was found. No promotion packet. |
| 2026-06-20 | 164 | DEFER | desktop MCP import dialog / conversation-decorations gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing app-server external-agent import already handles MCP config migration from repo/home settings into `mcp_servers`, with tests for MCP config import, settings toggles, project config, and invalid settings behavior. No fresh bug/regression/security/safety/product evidence or concrete desktop-import fixture gap was found. No promotion packet. |
| 2026-06-20 | 165 | DEFER | IDE auto-connect onboarding / rich-media-non-core gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing TUI onboarding, `/ide` context helpers, app-server remote-control pairing, and onboarding snapshot coverage already own adjacent behavior. No fresh bug/regression/security/safety/product evidence or concrete feature-gated bridge onboarding snapshot gap was found. IDE auto-connect onboarding would add new bridge setup, auto-connect, onboarding, config/API, pairing, or rich-media/desktop surface. No promotion packet. |
| 2026-06-20 | 166 | NARROW | IDE selection context indicator / diagnostics-display gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing `/ide` wiring, IDE prompt rendering for active file/selection/open tabs, footer IDE-context indicator snapshots, and composer transcript hiding tests already cover adjacent behavior. No exactly-one existing TUI/context diagnostics display snapshot or status gap was found without adding a new IDE/context/status UI surface. No promotion packet. |
| 2026-06-20 | 167 | NARROW | clickable file refs in TUI / compact status-line gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing TUI markdown rendering already owns local file-link display, path normalization, and file-link snapshots, while terminal OSC8 hyperlink support is web-link oriented. Making file refs clickable would require new terminal hyperlink/open-file behavior or navigation semantics, not exactly one owner-local markdown snapshot gap. No promotion packet. |
| 2026-06-20 | 168 | NARROW | structured diff component / existing TUI diff-pattern gate | parked | `gpt-5.4-mini` fallback | Stays parked. `diff_render.rs` already owns `DiffSummary`, `create_diff_summary`, `FileChange` rendering, wrapping, syntax highlighting, theme handling, and diff gallery snapshots; `/diff` already routes through the same summary path. No exactly-one existing diff-rendering snapshot gap was found, and a new structured diff component would add a second abstraction/renderer. No promotion packet. |
| 2026-06-20 | 169 | DEFER | virtual message list / rendering-abstraction churn gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing history cells, transcript overlay, resize reflow, row caps, streaming chunking, source-backed consolidation, and render regression tests already cover adjacent long-session rendering behavior. No fresh bug/regression/security/safety/product evidence or concrete owner-local performance regression gap was found; virtual message list would add a new rendering abstraction. No promotion packet. |
| 2026-06-20 | 170 | DEFER | offscreen freeze / cosmetic-theme gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing cached live-tail overlay rendering, resize reflow row caps, streaming chunking, table holdback/stable-prefix cache, and resize/streaming regression tests already cover adjacent render-cost hot paths. No fresh performance/product evidence or concrete owner-local performance smoke gap was found; offscreen freeze would add a new rendering subsystem. No promotion packet. |
| 2026-06-20 | 171 | DEFER | FPS metrics context / layout-experiment gate | parked | `gpt-5.4-mini` fallback | Stays parked. `/debug-config`, runtime-metrics logging, OTEL metrics tests, config schema coverage, and 120fps frame-requester rate-limit tests already cover adjacent diagnostics and render scheduling. No fresh performance/product evidence or concrete owner-local metrics gap was found; an FPS-facing diagnostics/context lens would add new telemetry, UI, model-context, or render-metrics surface. No promotion packet. |
| 2026-06-20 | 172 | NARROW | token warning component / error-topology display gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing auto-compaction token status, TUI bottom-pane context indicator, status-card context-window display, context-window error handling, and pre-turn compaction snapshots already cover adjacent token-pressure/error behavior. No exactly-one existing-owner token warning or structured-error display gap was found; a new warning component would add UI/context/compaction surface. No promotion packet. |
| 2026-06-20 | 173 | DEFER | cost threshold dialog / onboarding-non-core gate | parked | `gpt-5.4-mini` fallback | Stays parked. `/status`, status-card token/rate-limit/credits display, rate-limit warnings, lower-cost model switch prompts, owner add-credits nudge flows, app-server spend-control types, and usage-limit runtime tests already cover adjacent spend and usage visibility. No fresh spend-control safety/product evidence or concrete owner-local gap was found; a cost threshold dialog would add new UI/policy/API surface. No promotion packet. |
| 2026-06-20 | 174 | NARROW | rate-limit option command/UI / diagnostics-pane gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing backend rate-limit fixture parsing, `/status` rate-limit card rendering/refresh snapshots, chatwidget warning and switch-prompt tests, retry-after parsing tests, usage-limit error formatting, and debug-config rendering tests already cover adjacent diagnostics. No exactly-one owner-local fixture gap was found; a command/options UI would add new surface. No promotion packet. |
| 2026-06-20 | 178 | NARROW | internal logging container ID / hook-status diagnostic gate | parked | `gpt-5.4-mini` fallback | Stays parked. OTEL resource attributes are already deliberately scoped to service/env and log-only host data with tests for host-name presence and trace omission; hook browser/status rendering has dedicated logic and snapshot coverage. No exactly-one OTEL/hook diagnostics/redaction gap was found, and container identity logging would add a new telemetry surface. No promotion packet. |
| 2026-06-20 | 179 | DEFER | package-manager auto-update wrapper / optional-session-UI gate | parked | `gpt-5.4-mini` fallback | Stays parked. Update actions already map npm, bun, brew, and standalone installers; `codex update`, update notice snapshots, update prompt/version probing, doctor update checks, and platform installer scripts already cover adjacent update UX and platform behavior. No fresh install/update safety evidence or owner-local platform-gated gap was found; wrapper UI would duplicate existing update plumbing. No promotion packet. |
| 2026-06-20 | 180 | DEFER | native auto-updater abstraction / tooling-first UI-eval gate | parked | `gpt-5.4-mini` fallback | Stays parked. `install-context` already detects install channels/package layouts, TUI update actions map those channels to concrete commands, doctor update checks verify freshness and npm target safety, `codex update` uses the existing path, and install-context tests cover package layouts/channel precedence. No fresh update safety evidence or one config gap was found; a native updater abstraction would add new architecture. No promotion packet. |
| 2026-06-20 | 181 | DEFER | devcontainer firewall init scripts / release-automation gate | parked | `gpt-5.4-mini` fallback | Stays parked. Secure devcontainer config already wires firewall env, allowed domains, GitHub meta ranges, and `postStartCommand`; post-start validation calls the firewall script; `init-firewall.sh` implements allowlist, IPv6 default-deny, and verification; README documents outbound controls. No fresh network-safety evidence or devcontainer static-review gap was found. No promotion packet. |
| 2026-06-20 | 182 | DEFER | strict/lax settings examples / docs-process gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing permission-profile docs/examples already cover `permissionProfile/list`, the `permissionProfile` versus `sandboxPolicy` boundary, and concrete network/filesystem permission profile examples; memory doc-link-check tooling already exists. No fresh safety evidence or one broken/missing owner-local docs gap was found. No promotion packet. |
| 2026-06-20 | 183 | NARROW | managed settings example / config-docs schema gate | parked | `gpt-5.4-mini` fallback | Stays parked. Enterprise-managed config already has a config bundle owner, strict config validation coverage, generated config-schema fixture checks, loader README layering docs, debug-config enterprise-managed rendering, and app-server config/requirements read/write docs. No exactly-one docs/schema validation gap was found; this remains docs polish, not implementation scope. |
| 2026-06-20 | 184 | DEFER | issue templates for model behavior vs bugs / repo-automation gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing issue forms cover app, extension, CLI, other bugs, feature requests, and docs; contributing docs route bugs and behavior changes through issues; issue-labeler already includes `model-behavior`, and issue-deduplicator already covers duplicate triage. No fresh repo-triage safety evidence or one template/lint gap was found. |
| 2026-06-20 | 185 | DEFER | funding/security docs separation / repo-governance docs gate | parked | `gpt-5.4-mini` fallback | Senior accepted one concrete docs-index gap and added a README link to the existing `SECURITY.md`. Broader funding/security docs separation stays parked: `SECURITY.md`, `docs/open-source-fund.md`, contributing security guidance, and memory doc-link tooling already exist; no package/release/security-process/funding-workflow scope was added. |
| 2026-06-20 | 186 | DEFER | changelog/feed generation / release-automation gate | parked | `gpt-5.4-mini` fallback | Stays parked. `CHANGELOG.md` already points to GitHub Releases, release workflow generates notes from tag commit messages, installer latest-version resolution consumes GitHub release metadata, and TUI update notices link to latest release notes. No fresh release-consumption evidence or one generated-feed test gap was found without adding release automation. |
| 2026-06-20 | 187 | DEFER | build-bundle script with shims / packaging-smoke gate | parked | `gpt-5.4-mini` fallback | Senior accepted one concrete existing package-builder defect and removed duplicate Windows helper binary entries from `source_binaries_for_target`. Broader build-bundle/shim scope stays parked because package builder, release archive wrapper, DotSlash shims, layout validation, archive tests, and CI smoke already exist. `python3 -m unittest discover -s scripts/codex_package -p 'test_*.py'` and `CARGO_BUILD_JOBS=8 just fmt` passed. |
| 2026-06-20 | 188 | DEFER | Bun/Node runtime shim audit / packaging-script gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing Node shim detects Bun vs npm, sets the matching managed-install env var, and emits package-manager-specific reinstall guidance; install-context models `InstallMethod::Npm` and `InstallMethod::Bun` with precedence tests; Unix and PowerShell installers classify Bun/NPM conflicts and use matching uninstall commands. Existing staging/package-layout tests cover package artifacts. No fresh packaging/runtime-shim evidence or one concrete script-test gap was found. No promotion packet. |
| 2026-06-20 | 189 | DEFER | package-npm artifact checks / release-engineering gate | parked | `gpt-5.4-mini` fallback | Stays parked. `build_npm_package.py` already stages npm package contents, runs `npm pack --json`, parses the result, verifies the tarball exists, and moves it to `--pack-output`; `stage_npm_packages.py` invokes that pack-output path for release staging; CI stages and uploads an npm package artifact; release publishing refuses missing required platform/root tarballs and rejects unexpected tarballs. No concrete dry-run package-test gap was found without widening into release engineering. No promotion packet. |
| 2026-06-20 | 190 | DEFER | CI build script wrapper / dependency-audit gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing wrappers already cover the narrow build alignment surface: `scripts/build_codex_package.py` is the executable package-builder wrapper, `scripts/stage_npm_packages.py` is the CI-facing npm staging wrapper, `.github/scripts/run-bazel-ci.sh` wraps Bazel CI/local fallback behavior, CI workflows call those wrappers, and `justfile` exposes local recipes including GitHub-script tests and release binary builds. Dependency-audit checks already live in cargo-deny/cargo-shear workflows. No concrete CI dry-run gap was found. No promotion packet. |
| 2026-06-20 | 191 | NARROW | slash-command test scripts / plugin-packaging-docs gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing slash-command owners already cover completion/parsing, queued dispatch, unavailable-command recall, command-specific dispatch behavior, status command behavior, command popup rendering, and snapshots. The donor asks for a separate `test-commands` script, but no one concrete failing owner-local command regression gap was found without adding a new script/test surface or drifting away from the ADR's plugin-packaging-docs scope. No promotion packet. |
| 2026-06-20 | 192 | NARROW | MCP test script smoke / plugin-permission-checks gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing MCP/plugin owners already cover the narrow permission path: MCP auto-approval logic checks approval policy and permission profile, mod tests cover unrestricted/read-only profiles and tool approval overrides, plugin loader applies per-plugin MCP server policy, and manager tests exercise `.mcp.json` plus config overlays. Existing CLI/MCP-server/RMCP tests cover MCP smoke behavior. A new MCP test script would add a separate script surface rather than close one proven owner-local gap. No promotion packet. |
| 2026-06-20 | 193 | NARROW | auth test script smoke / skill-loading-diagnostics gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing auth owners already cover CLI API-key login, invalid access-token rejection, auth.json overwrite/token writes, login server e2e persistence, device-code login, logout/revoke, refresh, and storage behavior. Existing `core-skills` owners already carry structured load errors and warning messages, with tests for invalid metadata, missing frontmatter, truncation, and omission warnings. A new auth smoke script would add a separate script surface rather than close one proven auth or skill-diagnostics gap. No promotion packet. |
| 2026-06-20 | 194 | NARROW | services-layer test script / plugin-cache-validation gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing plugin-cache owners already test requested cache versioning, curated cache refresh/reinstall/no-op/migration behavior, and app-server plugin install/list cache wiring. App-server service tests already cover config/service request processors broadly. A new services-layer smoke script would add a separate script surface rather than close one proven plugin-cache or app-server service wiring gap. No promotion packet. |
| 2026-06-20 | 195 | DEFER | plugin development guide / test-matrix planning gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing plugin authoring/update guidance already lives in the `plugin-creator` skill, including scaffold, marketplace, reinstall, and validation flow; runtime plugin owners already cover marketplace layout and plugin discoverability tests. The donor asks for `plugins/README.md` plus link check, but no fresh bug/regression/product evidence or one owner-local broken docs/link gap was found. No promotion packet. |
| 2026-06-20 | 196 | DEFER | plugin-specific README template / marketplace-publishing gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing plugin-creator scaffold and validator cover the real plugin manifest/folder contract, while core plugin runtime owners load manifest/interface/path fields and reject bundles without the standard plugin root. A plugin README template plus template lint would add docs/marketplace-publishing process surface, and no fresh bug/regression/product evidence or one existing-owner README-template defect was found. No promotion packet. |
| 2026-06-20 | 197 | NARROW | code-review plugin as extension / existing plugin-skill owners gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing code-review skills already own review workflow guidance, and plugin/skill owners already cover plugin skill namespacing, explicit plugin mention injection, plugin section rendering, bundled-skill inclusion, and plugin-skill config filtering. Adding a new code-review plugin would create duplicate extension packaging rather than closing one existing owner-local fixture gap. No promotion packet. |
| 2026-06-20 | 198 | DEFER | feature-dev plugin workflow / extension-certification gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing skill-creator guidance already frames skills as reusable workflows with scripts/references/assets and a workflow-based template, while plugin-creator already owns plugin scaffolding and optional skill folders. Skill/plugin loading tests already cover roots and plugin fixture behavior. A feature-dev plugin would add generic workflow packaging under a deferred certification gate, not one current-owner defect. No promotion packet. |
| 2026-06-20 | 199 | DEFER | frontend-design plugin workflow / analytics-privacy ADR gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing model instructions and repo TUI rules already cover frontend/TUI guidance, while skill/plugin creator assets already cover workflow scaffolding, UI-facing skill metadata, embedded samples, and loading tests. A new frontend-design plugin would add opt-in workflow packaging under a deferred analytics/privacy ADR gate, not one current-owner fixture gap. No promotion packet. |
| 2026-06-20 | 200 | NARROW | simplification/senior-review plugin workflow / review-skill gate | parked | `gpt-5.4-mini` fallback | Stays parked. Existing code-review skills already cover review orchestration, testing guidance, and change-size review; the Ponytail plugin already provides over-engineering and simplification review/audit workflows outside core. Plugin/skill fixture coverage already exists for plugin skills, hooks, and config filtering. Adding a new simplification/senior-review workflow would duplicate current plugin/skill surfaces rather than close one owner-local gap. No promotion packet. |
| 2026-06-20 | 121 | REJECT | MCP explorer server / OntoIndex source-exploration gate | rejected | `gpt-5.4-mini` fallback | Stays rejected. OntoIndex is the approved code-exploration path, while existing MCP owners already cover tool/resource list/read specs, connection-manager tool listing, cached MCP tool metadata, and deferred MCP tool discovery through `tool_search`. A new MCP explorer server would clone source browsing and add a parallel dev-tooling owner. No promotion packet. |
| 2026-06-20 | 124 | REJECT | get-tool-source MCP endpoint / raw-code exposure gate | rejected | `gpt-5.4-mini` fallback | Stays rejected. Existing MCP discovery/resource owners already cover `tool_search`, MCP resource list/read specs, and spec tests, while `codex-mcp` keeps raw tool identities internal for routing and exposes sanitized model-facing names. A dev-gated raw-source endpoint would add a leakage-prone source API rather than close one existing-owner defect. No promotion packet. |
| 2026-06-20 | 125 | REJECT | get-command-source MCP endpoint / OntoIndex symbol-search gate | rejected | `gpt-5.4-mini` fallback | Stays rejected. Slash-command metadata/discovery/dispatch already live in the TUI command owner, and OntoIndex remains the approved code/symbol exploration path. A dev-gated command-source MCP endpoint would create a parallel source-browsing surface rather than close one proven command-review or dev-permission defect. No promotion packet. |
| 2026-06-20 | 126 | REJECT | read-source-file MCP endpoint / existing file-search gate | rejected | `gpt-5.4-mini` fallback | Stays rejected. Existing MCP resource reading is server/URI scoped, `tool_search` is the model-facing MCP discovery path, and file exploration already has file-search, shell search, and OntoIndex owners. An allowlisted source-file MCP endpoint would add a parallel filesystem source API rather than close one proven path-traversal or allowlist defect. No promotion packet. |
| 2026-06-20 | 127 | REJECT | search-source MCP endpoint / grep-wrapper gate | rejected | `gpt-5.4-mini` fallback | Stays rejected. `tool_search` already exposes bounded tool discovery search, `file-search` owns repository search with limits and truncation warnings, and exact text search can use existing shell/`rg` paths. A new MCP `rg` wrapper would create parallel grep/search surface rather than close one proven result-cap or search behavior defect. No promotion packet. |

## Stop Conditions

Stop and ask for review if the task needs:

- code changes,
- a new public API/config/schema,
- a new context fragment,
- a new MCP, hook, job, plugin, eval, or UI owner,
- changes to `KEEP` rows in the parent ADR,
- or more than one parked row.
